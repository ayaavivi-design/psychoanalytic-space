<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# עריכת פרומפטים: דוגמה מנצחת איסור

**לפני שמוסיפים איסור לפרומפט, חובה לסרוק את הקובץ אחרי דוגמאות שמדגימות את ההפך. איסור בלי דוגמה, מול הדגמה עובדת של אותה התנהגות, מפסיד.**

הכלל נכתב אחרי שלושה מקרים שקרו באותו יום (23.08.2026) ועלו לנו שבועות:

- **ויניקוט ציטט את ויניקוט בגוף שלישי.** הסרנו שורה שהדגימה זאת ב-21.08, וזה חזר. הסיבה: `rag.ts` ביקש "quote directly with attribution" מעל קטעים שכותרתם ספריו שלו, ולא היה איסור מפורש בשום מקום. **הסרת דוגמה אינה חוק.**
- **הד מילת המטופלת (8c).** ליה המליצה על אותה שורה שלוש פעמים והיא כבר הייתה בקוד. מה שסתר אותה: שלוש דוגמאות באותו קובץ שלימדו את ההד כ-RIGHT.
- **תבנית המקף אצל ויניקוט.** כלל 2 אוסר הד, והמודל מצא צורה תחבירית שמחזירה אותו בלבוש של מהלך.

**איך לעבוד:**

1. לנסח את האיסור.
2. **לגרפ בקובץ** אחרי המונח, אחרי `RIGHT:` ו-`WRONG:`, ואחרי דוגמאות שמדגימות את ההתנהגות האסורה כרצויה.
3. לשכתב את הדוגמאות הסותרות, לא רק להוסיף את האיסור מעליהן.
4. **לבדוק גם את גושי המצבים ואת ה-RAG**, לא רק את בלוק הקול. סריקה שכיסתה רק `theorist-voices.ts` פספסה שני מקורות מתוך שלושה.
5. סדר ההרכבה קובע: מה שנקרא אחרון ומכריז על עצמו כגובר, גובר. איסור בראש הפרומפט מול "FINAL CHECK" בסופו יפסיד.

**וכלל משלים:** אין לסגור פריט על סמך עריכת קוד בלבד. "הקוד תוקן" אינו "התיקון עובד". פריט קול נסגר בריצה חיה על החומר שהפיל אותו.

# בעלות על אלמנטים: מי שמרנדר הוא הבעלים

**הכלל, ולפניו אין דיון:** אלמנט שייך לצד אחד בלבד. **React מרנדר, ולכן React הוא הבעלים.** `chat.js` אינו כותב לאלמנט ש-React מרנדר, ולא ל-`innerHTML`, ולא ל-`textContent`, ולא ל-`value`, ולא ל-`classList`, ולא ל-`style`. **החריג היחיד: אלמנטים הנושאים `data-key`**, שהם של vanilla בהסכמה.

הכלל נכתב אחרי **אחת עשרה** הפרות מתועדות (פריט 17 ב-`OPEN_LOOPS`), וזה מה שהן לימדו:

- **הן אינן נראות.** ארבע מהשש הראשונות חיו חודשים ונחשפו רק כי שינוי אחר הזיז אותן לתנאים שבהם הן מתפרצות. **הדיברגנציה אינה יוצרת את הבאג, היא מסתירה אותו.**
- **הן חוזרות.** חמש נוספות נמצאו ביום אחד, 29.08, אחרי שהראשונות תוקנו.
- **התיקון תמיד זהה:** React מרנדר, ולכן `applyUITranslation` וכל כותב אחר ב-`chat.js` מפסיקים לגעת.

**לפני שכותבים ל-`document.getElementById(...)` ב-`chat.js`:** לגרפ את המזהה ב-`app/page.tsx`. אם הוא שם, **לא כותבים** — מעבירים את הערך ל-React דרך אירוע או דרך `props`.

**הבדיקה:**

```bash
node scripts/check-ownership.mjs
```

היא מוצאת כל מזהה שגם React מרנדר וגם `chat.js` כותב אליו. `scripts/ownership-baseline.json` מחזיק את **28 המקרים שהיו ביום שהיא נכתבה** (30.08.2026), והבדיקה **נכשלת רק על חדש**. זו הכוונה: הרשימה הקיימת היא חוב מתועד, לא היתר. מי שמתקן אחד מוחק את השורה מהקובץ, והבדיקה לא תיתן לו לחזור.

**מה היא לא תופסת:** כתיבה דרך `querySelector` עם מחלקה, או דרך מזהה שנבנה ממשתנה. היא תופסת את הדפוס שחזר אחת עשרה פעמים, ולא את כל מה שאפשר.

# כלל גר במקום אחד

**הכלל, ולפניו אין דיון:** כלל שנכתב **בשלושה קולות** אינו כלל של קול. הוא עובר לשכבה משותפת **ונמחק משלושתם**. וכלל שכבר יושב בשכבה משותפת **אינו נכתב שוב בקול**: מי שצריך אותו חזק יותר מתקן אותו שם, ולא מדביק עותק.

שלוש שכבות, וכל כלל שייך לאחת בלבד:

| שכבה | המבחן | דוגמאות |
|---|---|---|
| **`CORE_GUARDRAILS`** | "האם ניסוח שונה לפרויד ולביון היה משנה משהו?" **לא** | מגדר · שפה · דליפת תווית מצב · שאלת זהות · סיפור עצמי · המצאה · חנופה · הוראות במה |
| **מסגרת המטופלת** | "האם `CONSULT_SUFFIX` כבר משעה את זה בסיטונות?" **כן** | הצבעה חזרה לחדר · החזק־והחזר־פעם־אחת · בלי בינארי |
| **בלוק הקול** | "האם ויניקוט היה אומר את זה אחרת?" **כן** | מכונת המצבים · ההקשבה האבחנתית · הדוגמאות המעובדות · הסגירה · מבחן המובחנות |

הכלל נכתב אחרי מדידה של הקובץ כולו, 31.08.2026, וזה מה שהיא הראתה:

- **הכללים אינם מפוזרים לפי מה שהקול צריך, אלא לפי מתי נגענו בו לאחרונה.** פרויד, קליין, ויניקוט ואוגדן קיבלו כל תיקון; לוואלד, ביון, קוהוט והיימן לא. **אותה חבורה בדיוק, שש פעמים.**
- **החילוץ כבר קרה. המחיקה לא.** חמישה כללים כבר יושבים ב-`CORE_GUARDRAILS`, **ואף אחד מהם לא נמחק מהקולות**: גיוון פתיחות (ועוד 8 קולות) · ביטויי התאוששות (6) · לא כל תור נגמר בשאלה (5) · תבנית המקף · משהו משלך בכל תור.
- **וההצטברות מסתירה חורים.** שמונה קולות מריצים את מכונת המצבים ורק חמישה מוגנים מדליפת התווית. **שאלת זהות חסרה בחמישה ואינה ב-`CORE` כלל.**

**למה זה קרה, וזה החלק שהכלל לבדו אינו פותר:** **מחיקה אינה ניתנת לאימות.** אפשר להראות שהוספת שורה תיקנה משהו; אי אפשר להראות שהסרת שורה לא שברה משהו. לכן כל תיקון נכתב כתוספת. לכן **מוחקים רק מה שיש עליו כיסוי ריצה** (`/eval`), בדיוק כפי שכתוב למעלה: "פריט קול נסגר בריצה חיה על החומר שהפיל אותו".

**ואזהרה על מיקום, כי הזזה אינה ניטרלית:** `CORE_GUARDRAILS` נקרא **אחרי** בלוק הקול, ולכן העברת כלל אליו **מחזקת אותו** מול הקול ולא רק מבטלת כפילות. אין לעשות זאת בלי ריצה חיה לפני ואחרי. (`CORE_GUARDRAILS` אינו אחרון בפרומפט כולו: אחריו עוד באים הקשר המשתמש ו-RAG או בלוק ההיקף. הוא אחרון בבלוק הסטטי, ומשם משני שיקולים תפעוליים — שלא ייפול כשה-RAG מצליח, ושהמטמון יישאר חם.)

**הבדיקה:**

```bash
node scripts/check-voice-duplication.mjs
```

היא מוצאת שתי הפרות: **כלל שיושב ב-`CORE_GUARDRAILS` וגם בבלוק קול**, וכלל שיושב **בשלושה קולות ואינו בשכבה משותפת**. `scripts/voice-baseline.json` מחזיק את **19 ו-17 המקרים שהיו ביום שהיא נכתבה** (31.08.2026), והבדיקה **נכשלת רק על חדש**. זו הכוונה, כמו בבדיקת הבעלות: הרשימה הקיימת היא חוב מתועד ולא היתר. מי שמתקן אחד מוחק את שורתו מהקובץ, והבדיקה לא תיתן לו לחזור.

**מה היא לא תופסת:** כלל שנוסח מחדש במילים אחרות ואינו במילון שבראש הסקריפט. סריקת גילוי בסוף הריצה מציעה מועמדים חדשים למילון, **ולעולם אינה מפילה את הבדיקה**.

## ורה ואליוט אינם בתחום של תיקון גורף

**הם מוקפאים מ-29.08.2026 בהכרעת איה, והקוד שלהם חי.** משמעות הצירוף הזה: **תיקון שנעשה "על כל הקולות" יגלוש אליהם אלא אם יוחרגו במפורש.**

**הכלל:** כל סקריפט או עריכה שרצים על `THEORIST_VOICE` **מחריגים את `vera` ו-`elliot`**, אלא אם איה אמרה אחרת באותה שיחה.

**זה קרה ב-31.08:** מחיקת כפילויות כללה אותם, ואיה עצרה. השורות שלהם ברשימת הכפילות נשארות **חוב מתועד** ואינן נמחקות עד שיוקפצו.
