# UX-RULES: העיצוב של Between

_עודכן 29.08.2026 בהכרעת איה. **מקור האמת הוא קובץ אחד: `.impeccable/fork/index.html`.**_
_הקובץ נטען בכל שיחה דרך `CLAUDE.md`. כל סוכן שנוגע בעיצוב קורא אותו לפני שמתחיל._

---

## הכלל הראשון, ולפניו אין כלום

**מסמכי עיצוב ישנים סומנו כמוחלפים 29.08.2026** ואינם בתוקף: `docs/design-system.md`, `docs/therapist-hub-spec.md`, `docs/MOBILE-HEADER-SPEC-2026-07-23.md`, `bizdev/presentation-design-spec-2026-05.md`. הם מתארים רקע קרם, קורמורנט ורוביק — כולם הוחלפו. אין להסיק מהם ערך.

**דוחות מתוארכים אינם הוראות.** `daily-summaries`, `judge-analysis`, `release-reports`, `onboarding-reports` ו-`marketing-reports` הם רישום של מה שהיה. הם מזכירים פונטים וצבעים ישנים, ואין לתקן אותם ואין לפעול לפיהם.

**סקיל `impeccable` נשאר זמין, אבל לא כדי לעצב מחדש.** ההוראה שלו לבחור עולם ויזואלי חדש **אינה חלה כאן**: העולם נבחר ואושר, ואיה מנחה אותו ידנית. כל בקשה בפרויקט הזה היא refinement בתוך העולם הקיים, גם כשהיא מנוסחת כ"עצב מחדש".

**המנגנון שאוכף את זה הוא `DESIGN.md` בשורש.** הסקיל קורא אותו לפני שהוא פועל, וההוראה שלו עצמה אומרת שהיעדר הקובץ אינו הופך פרויקט לשדה בתולי. הקובץ לא היה קיים, ולכן הסקיל בחר עולם בעצמו. עכשיו הוא קיים ומצהיר שהעולם סגור. **אין להחליף את `DESIGN.md`** — זו הכרעה של איה בלבד.

**אין סוכן או פקודה שיכולים לעצב מחדש.** נותרו ארבע פקודות תהליך: `/jira`, `/eval`, `/handover`, `/verify-finding`.

**`.impeccable/fork/index.html` הוא העיצוב.** כל הפרש בינו לבין הממשק המקומי הוא **באג**, לא פרשנות ולא שיפור.

- אין להעלות ערך שאינו בקובץ, גם אם הוא נראה טוב יותר.
- אין להוריד ערך שיש בקובץ, גם אם הוא נראה בעייתי.
- מי שחושב שמשהו בקובץ שגוי — **אומר לאיה ולא מתקן**. ההכרעה שלה, ואז הקובץ מתעדכן ראשון.
- הטמעה נעשית בהעתקה, לא בתרגום. כל שכתוב של מבנה או שינוי שם מחלקה הוא הזדמנות לסטות.

**איך בודקים:** מודדים. פותחים את הקובץ ואת הלוקאל באותו רוחב, ומשווים `getComputedStyle` על אותם אלמנטים. "נראה דומה" אינו בדיקה.

---

## הטוקנים · הערכים בקובץ

**ורוד המותג**
`--p-50 #FDF2F5` · `--p-100 #F8DDE4` · `--p-200 #ECC7D4` · `--p-300 #D4899A` · `--p-400 #CE6F87`
`--p-500 #C4607A` מותג, סמן וסימון · `--p-hover #BA536F` ריחוף · `--p-600 #A8475F` מילוי עם טקסט לבן
`--p-700 #8E3550` טקסט ורוד על בהיר · `--p-800 #6B233A` · `--p-900 #4A1626` דיו התיאורטיקן

**הכלל שנגזר מזה, ואסור לשבור אותו:** `#C4607A` נותן 3.96:1 מול לבן ולכן **אינו יכול לשאת טקסט לבן**. מילוי עם טקסט לבן הוא `#A8475F` (5.63:1), וריחוף הוא `#BA536F`.

**ניטרלים**
`--bg #F4F4F2` · `--surface #FAFAF9` · `--zone #F0F0EE` · `--zone-off #E1E1DE`
`--line #D6D6D2` · `--line-soft #E6E6E2` · `--off #9A9A95`
`--field #FFFFFF` **כל משטח שכותבים לתוכו** · `--field-ph #6E6D69` פלייסהולדר
`--ink #111111` · `--mut #66655F`

**רדיוס** `--r-ctl 6px` פקדים · `--r-box 8px` קופסאות. פינה קטנה מאוד, לא חדה ולא עגולה.

**מרווח** 4 · 8 · 12 · 16 · 24 · 32 · 48

**גבהים** `--h-ctl 44px` מינימום מגע · `--w-side 248px` סייד־בר

**פונט** אסיסטנט בלבד, בששת המשקלים 200–700. אין משפחה שנייה.

---

## המבנה · שתי שפות מכל נפרדות

**אסור לערבב ביניהן.** מסך אינו מקבל כרום של מודל, ומודל אינו מקבל כרום של מסך.

**מסך:** בר עליון (11px, ריווח `.14em`, `space-between`) · קו שחור 1px · כותרת `h1.h` 38px משקל 200 · תוכן · פוטר 11.5px עם קו עליון שחור.

**מודל:** כותרת `h3` 21px משקל 300 · שורת פתיח `.lead` 12.5px · תוכן · סרגל פעולות תחתון דביק עם קו מפריד.

**עמודת התוכן היא 760 פיקסלים** וממורכזת. הבר העליון, הכותרת, הלוח, שורת הפעולות והפוטר יושבים כולם על אותו קו.

---

## הרכיבים

**כפתור** גובה 44 · ריפוד `0 20px` · 14px משקל 600 · רדיוס 6.
`ghost` מסגרת דיו · `solid` מילוי `--p-600` וטקסט לבן · `plain` מסגרת `--line` וטקסט `--mut` · `sm` גובה 34 וגודל 12.5.
במצב כבוי: `ghost`/`plain` יורדים ל-`--line` ו-`--off`; `solid` יורד לרקע `--line`.

**לוח הכתיבה** שני אזורים. **מעל** בחירת הגישה, ברקע `--zone`, ריפוד `20/23/15`. **מתחת** הכתיבה, ברקע `--field`, ריפוד `20/23`. השדה **נעול עד שנבחרה גישה**, ואז הרקע `--zone-off` והטקסט `--off`.

**התמליל** מסמך לבן אחד, בלי בועות ובלי צדדים. תור = עמודת שם דובר 74 פיקסלים (11px, משקל 700) לצד גוף (16px, משקל 300, גובה שורה 1.8, רוחב מרבי 60 תווים). ההבחנה בין הקולות היא **צבע הדיו בלבד**: הכתיבה שלה ב-`--ink`, התיאורטיקן ב-`--p-900`, ושמו ב-`--p-700`.

**כרטיס המקרה** רקע `--surface`, מסגרת `--line`, רדיוס `--r-box`, ריפוד `16/24`. השם 15px משקל 600, ומתחתיו שורת מטא 12px `--mut` במרווח 3.
**השורה הזו היא מה שאומר אם יש למה לחזור,** ולכן היא אינה תאריך בלבד: ספירת ההתייעצויות כתווית בקצה הפנימי (10.5px, משקל 700, ריווח `.14em`, `--p-700`), ואחריה **מתי עודכן** ו**דרך איזו גישה** — "עודכן לפני יומיים · אחרונה דרך הגישה של ויניקוט". מקרה בלי התייעצויות אומר זאת במפורש: "נפתח היום · עוד לא נכתב דבר".
**הזמן יחסי ולא מוחלט** ("לפני יומיים", לא "27 באוגוסט"), כי הכרטיס נסרק במבט ותאריך מוחלט דורש חישוב מהקורא. 29.08.2026.

**סייד־בר** רוחב 248 · פריט 13.5px משקל 500 בגובה 40 וריפוד `0 24px` · כותרת קבוצה 10px משקל 700 בריווח `.18em` · פריט משנה 13px משקל 400 בגובה 34 והזחה 42.
ערך מצב בפריט (למשל **מחקר**) יושב בקצה הפריט ב-11.5px משקל 600: **כבוי · בבחירה · פעיל**.
**רמה אחת בלבד, ואין כותרות קבוצה.** 29.08.2026. היו שלוש רמות על שישה פריטים: כותרת **מקרים**, מתחתיה **כל המקרים** שנקרא ככותרת שנייה אף שהוא פריט, ומתחתיה המקרים בהזחה 42. **ארכיון** ישב באותה הזחה של **כל המקרים** בלי להיות מאותו סוג. כל הפריטים יושבים עכשיו באותה הזחה (24), וההפרדה נעשית ב**קו דק** `--line-soft` בריפוד `10/24`.
**הקו מפריד בלי להוסיף רמה:** הוא אינו טקסט, אי אפשר ללחוץ עליו, והעין אינה קוראת אותו כשלב במבנה. זו הסיבה שהוא הוחלף בכותרות ולא להפך.
**סדר אצל מטפלת:** **מקרים** (הפריט הראשון, ומוביל לרשימה, והפלוס עליו) · המקרים · קו · **ארכיון** · קו · **מחקר**.
**אצל מטופלת:** **שיחה חדשה** · קו · **מה כתבתי**.
**"שיחת היכרות" הוסרה מה-UI.** 29.08.2026, הכרעת איה. הסריקה הראתה שמתוך תשע שאלותיה **שדה אחד בלבד נקרא אי פעם — לשון הפנייה**, ו-`intakeData` לעולם אינו נשלח לשרת. שאלה אחת אינה מצדיקה טופס בן תשע. **לשון הפנייה נשאלת בהגדרות**, ו-`buildUserContext` קורא אותה משם. הקוד נשאר תחת גידור (`false &&`), וההחזרה היא הסרת גידור.
**הכפתור מקרה חדש שבמסך "כל המקרים" נשאר:** הפלוס מלווה את הרשימה בכל מסך, והכפתור יושב בתוך המסך.
**לארכיון יש מסך משלו.** בלעדיו "העברה לארכיון" מזיזה מקרה למקום שאי אפשר לראות.

**הבר השחור** `#151515` עם קו `#2A2A2A`, ריפוד `10/18` וגובה `72`. **הלוגו בהתחלה וההגדרות בסוף** — בעברית לוגו בימין והגדרות בשמאל, באנגלית להפך. הלוגו בגובה 52 (**וזו אינה חזרה ל-52 של 29.08:** ב-30.08 נחתך ה-`viewBox` של הקובץ אל הדיו, ולכן אותו מספר נותן לוגו נראה גדול בהרבה. **גובה הבר נשאר 72**, ניסיון להרחיב אותו ל-104 נפסל), ומתג הפרסונות (**מטופלת · מטפלת**, כלי פיתוח המגודר ל-localhost) נדחף עם ההגדרות לקצה הנגדי.
**הלוגו ממורכז מעל עמודת הסייד־בר:** רוחב הסימן הוא `--w-side` (248), עם הזחה שלילית של 18 שמבטלת את ריפוד הבר באותו צד, ומרכוז בתוכו. כך מרכז הלוגו יושב על מרכז העמודה שמתחתיו. מתחת ל-900 אין סייד־בר בזרימה, והוא חוזר להיצמד לקצה.

---

## הכרעות שנרשמו

- **הכניסה היא מייל וסיסמה,** ולא קישור במייל. 29.08.2026.
- **מסך הכניסה ממורכז ושקט.** 30.08.2026, כיוון א׳. עמודה של **330** באמצע, **הלוגו** בגובה 52 במקום המילה (זה היה המסך היחיד בלי הסימן), משפט אחד, שני שדות, פעולה אחת, ושורת מעבר שקטה. **שלוש פסקאות האותיות הקטנות צומצמו לשורה אחת** שמתחלפת עם המצב: בהרשמה "גישת מטפלים בהזמנה בלבד כרגע", ובכניסה "השיחות נשמרות על המכשיר שלך".
- **"מי נכנסת" מופיע בהרשמה בלבד** וירד מהכניסה. **אין להסיר אותו לגמרי:** הפרסונה נקבעת ב-`(choice === 'therapist' && allowed)`, ולכן מטפלת שנמצאת ברשימת ההיתר ולא בחרה תיכנס כמטופלת. הבחירה נשאלת פעם אחת ונשמרת.
- **למצב שאחרי ההרשמה יש מסך:** אותה עמודה, בלי שדות, עם המייל שנשלח ו"לא הגיע? לשלוח שוב". הוא קיים בקוד מזמן ולא היה לו עיצוב.
- **המכל של הבורר נקרא `authseg` ולא `seg`.** `.seg` הוא כלל גלובלי שמעצב את **הכפתורים** בהגדרות (גובה 38, מסגרת, ריפוד), ומכל בשם הזה קיבל מסגרת של כפתור והכפתורים שבתוכו גלשו ממנה.
- **מסך ראשון אחרי הכניסה: "איך לפנות אלייך?"** 29.08.2026. כרום של מסך (בר עליון · קו · כותרת 38/200 · פוטר), שני שדות בלבד — שם או כינוי (לא חובה) ולשון פנייה כשלושה מגזרים בגובה 44 — ופעולה אחת, **אפשר להתחיל**. **אין דילוג:** בלי הערך הקול מנחש מגדר בשיחה הראשונה, וזו השיחה שקובעת אם היא נשארת.
  **מוצג פעם אחת, ורק בממשק עברי.** התנאי הוא היעדר לשון פנייה שמורה ולא "משתמשת חדשה", ולכן גם מי שנרשמה קודם ומעולם לא הגדירה תישאל פעם אחת. **באנגלית המסך אינו מוצג כלל:** `you` אינו מגדרי, וההוראה שהשרת שולח מנוסחת על צורות עבריות — מסך ששואל שם שאלה שאין לה תוצאה.
- **פעולה ראשית אחת בלבד במסך הכניסה.** משתמש חדש רואה **הרשמה**, משתמש חוזר רואה **כניסה**, לפי `bw_has_account`. שני משתמשים אמיתיים ניסו כניסה לפני שנרשמו.
- **שורת המעבר נשארת** מתחת לכפתור, שקטה. בלעדיה משתמשת שמנקה עוגיות נתקעת.
- **ההגדרות בבר העליון,** לא בסייד־בר. התפריט נפתח עם צל, ומסגרתו `--line` ולא דיו, כמו בממשק.
- **בראש התפריט כרטיס פרופיל,** בצורה של תפריט הפרופיל בכרום: סימן עגול 56 במילוי `--p-600`, ומתחתיו השם 15px משקל 600 והמייל 12.5px `--mut`, הכל ממורכז על אזור `--p-50`. 29.08.2026. **אינו כפתור.** העיגול הוא הצורה היחידה במערכת שאינה נושאת פינה של 6 או 8, והוא כאן משום שזו הצורה שנקראת ככרטיס משתמש.
- **פריטי התפריט הם אלה שבממשק** ובאותו סדר: חיפוש רשת (מטפלת בלבד) · שפה · צרי קשר · מפריד · הגדרות ופרופיל · התנתק.
- **הסייד־בר לניווט בלבד.** 29.08.2026. השם ירד מראשו וכרטיס החשבון ירד מתחתיתו, ושניהם יושבים עכשיו בבר ובתפריט שלו.
- **הלוגו הוא יונק דבש עם המילה `Between`.** 29.08.2026. **זה מבטל את הקפאת נושא הלוגו ואת "אין סימן גרפי" מ-28.08.** שתי גרסאות: כהה על בהיר, ולבן על כהה. בבר השחור נטענת גרסת הלבן. השם נשאר `Between` בכל שפה.
  **הקבצים:** `public/Between_logo_white.svg` (לבן, לרקע כהה) ו-`public/Between_logo_black.svg` (שחור, לרקע בהיר). **הם החליפו את `between-logo-*` ב-30.08.2026.**
  **הם נוקו ואינם המקור.** הקבצים שאיה שלחה, `*_on_transparent.svg`, אינם שקופים למרות שמם: 311,186 פיקסלים בהם באלפא 5 עד 8, ורק שלושה שקופים באמת. השכבה הזו נראית כמלבן בהיר על הבר השחור. בקבצים הנטענים כל פיקסל שאלפא שלו 20 ומטה אופס לגמרי, ואיתו גם ה-RGB שלו כדי שלא תיווצר הילה בהגדלה. **התמונה עצמה לא נגעה.** המקור נשמר בתיקייה ואם מגיעה גרסה נקייה היא מחליפה.
  **הגדלים:** בבר 52 פיקסלים, ובמסך הכניסה 50.
  **ואי אפשר להשוות אותם לגדלים שלפני 30.08.** בקובץ המקורי הדיו תפס 47% מגובה הקנבס בלבד (קנבס 768×422, דיו 481×199, ומעליו 93 פיקסלים ריקים ומתחתיו 130), ולכן כל הגדלה הגדילה בעיקר אוויר. ה-`viewBox` נחתך אל הדיו עם שוליים של 12, `0 0 768 422` ל-`163 81 505 223`. **התמונה עצמה לא נגעה**, זו הגדרת חיתוך והיא הפיכה בשורה אחת.
  **ה-`src` נושא `?v=2`.** בלעדיו הדפדפן מגיש את הקובץ הישן מהמטמון תחת אותו שם, וזה מה שקרה בפועל.
  **קבצי ה-PNG נשארו בתיקייה כגיבוי ואינם בשימוש.** הם נחתכו מקובץ המקור `Gemini_Generated_Image_axhi31axhi31axhi.jpeg`, שאינו נכנס למעקב ואינו מוגש (ראה `.gitignore`).
  **הפרש שנרשם בהחלפה:** ציפור הדבש חופפת כמעט לגמרי, והמילה `Between` ב-SVG צרה וקלה מעט מזו שב-PNG. איה ראתה את ההשוואה מוגדלת ובחרה ב-SVG.

---

## פסילות שנרשמו

_פידבק שניתן פעם אחת. אין להציע שוב ואין לבקש נימוק מחדש._

- **סימן הסוגריים כלוגו.** נפסל 28.08.2026.
- **לוגו בעברית או לוקאפ דו־לשוני.** נפסל 28.08.2026. **הלוגו הוא `Between` תמיד**, גם בממשק עברי.
- **לצייר את יונק הדבש ביד.** נפסל 28.08.2026 אחרי שני ניסיונות שעיוותו את הרפרנס. **הלוגו נכנס כקובץ מהמקור בלבד.** אין לשרטט אותו, לא ב-SVG ולא בקוד.
- **קו אנכי בשוליים של תור, כסימן מי מדבר.** נפסל 28.08.2026. ההבחנה היא צבע הדיו.
- **שורה נפרדת לכפתור "שיתוף עם המטפל/ת".** נפסל 29.08.2026. הוא יושב ראשון בשורת הפעולות שמתחת ללוח הכתיבה, כמו בממשק.
- **רצפת קריאוּת שמרימה גדלים מעל הקובץ.** נפסל 29.08.2026. הועלתה על ידי סוכן ולא בבקשת איה, והרימה כל טקסט של 10 עד 13.5 פיקסלים ל-13 ו-15. **זו הייתה הסטייה הגדולה ביותר בין הלוקאל לעיצוב.** הגדלים הם מה שכתוב בקובץ.

---

## מה עדיין נדרש

1. **לומר מה השתנה,** בשורה או שתיים. מידע להחלטה של איה, לא פסילה.
2. **לא לטעון שמשהו נבדק אם לא נבדק.** לחיצה תכנותית (`el.click()`) עוקפת בדיקת נגיעה ומדווחת הצלחה גם כששכבה מכסה את הפקד. **בודקים דרך `elementFromPoint`.**
3. **מדידה על אלמנט מוסתר מחזירה אפסים** שנראים כמו כישלון. לוודא רוחב גדול מאפס לפני שמסיקים.
4. **פידבק עיצובי נכנס לקובץ הזה,** פידבק על קוד נכנס ל-`MEMORY.md`.

---

## הגבול היחיד שאינו של העיצוב

הקווים האדומים ב-`CORE.md` הם מוצר וקליניקה: המרחב אינו תחליף לטיפול ואינו מציג את עצמו ככזה. העיצוב נוגע לצורה, לא למה שהמרחב **אומר** למטופלת.
