# UX-RULES — כללי עיצוב שנקבעו על ידי איה

_קובץ זה הוא מקור האמת לכללי UX. כל סוכן שנוגע בעיצוב קורא אותו לפני שמתחיל._
_כשנוסף כלל חדש — מוסיפים אותו כאן ולא שולחים פידבק חוזר._

---

## כלל 1 — אל תמציאי עיצוב שלא בדיזיין סיסטם

**הכלל:** כל component, צבע, border-radius, פונט, ריווח ואנימציה חייבים להגיע מ-`docs/between-tokens.json` או מ-`app/globals.css`.
**אסור:** להמציא tooltip חדש, בועה חדשה, כרטיס חדש, או כל אלמנט שלא קיים כבר.
**מותר:** להשתמש ברכיבים קיימים במיקום חדש (למשל: tooltip שקיים בסייד-בר → להזיז לנקודה אחרת).

---

## כלל 2 — כל ערך חייב להיות ממופה לטוקן

**הכלל:** בכל ביקורת עיצובית — כל ערך שנמדד (padding, font-size, border-radius, color) חייב להיות מטוקן מ-`docs/between-tokens.json`.
**ערך שלא קיים ברשימה = סטייה שחייבת דיווח.**
**פורמט:** `❌ .selector prop: VALUE — צריך TOKEN=VALUE (דלתא: Xpx)`

---

## כלל 3 — screenshot לפני ואחרי כל שינוי

**הכלל:** לא לדווח "מוכן" בלי לבדוק בפועל. צלמי screenshot לפני ואחרי כל שינוי שמשפיע על מה שהמשתמש רואה.
**אם לא צולם — השינוי לא אומת.**

---

## כלל 4 — preview_eval הוא prototype בלבד

**הכלל:** CSS שמוזרק דרך `preview_eval` הוא זמני ולא חלק מהקוד. לא לדווח שינוי כ"מיושם" אם הוא רק ב-eval.
**כשמאשרים עיצוב** — הוא עובר לקוד אמיתי (`globals.css`, `chat.js`, `page.tsx`).

---

## כלל 5 — פידבק שניתן פעם אחת לא יחזור

**הכלל:** כשאיה נותנת פידבק — מיישמים אותו, זוכרים אותו, ולא מחכים לפידבק חוזר.
**אם הפידבק קשור לעיצוב** — מוסיפים אותו לקובץ הזה.
**אם הפידבק קשור לקוד** — מוסיפים אותו ל-MEMORY.md תחת "מה לא עבד".

---

## כלל 6 — אחידות מערכת לפני חידוש

**הכלל:** לפני שמוסיפים component חדש — בדקי שלא קיים כבר משהו דומה. שימוש ב-component קיים עדיף תמיד על יצירה מחדש.
**שאלת הבדיקה:** "האם משהו דומה כבר קיים בממשק?"

---

## כלל 7 — tooltip קיים = להשתמש בו, לא להמציא חדש

**הכלל:** ה-tooltip העשיר (approach/concepts/forWhom) מוגדר ב-`page.tsx` כ-React component. זה ה-tooltip היחיד המאושר לתיאורטיקנים.
**לא ליצור:** bועות CSS חדשות, `::after` tooltips, floating divs — אלא אם כן אושר במפורש.

---

## כלל 8 — אל תכסי טקסטים

**הכלל:** שום אלמנט (tooltip, dropdown, overlay, card) לא מותר שיכסה טקסט שהמשתמש צריך לקרוא.
**בדיקה:** לפני כל positioning — ודאי שהאלמנט לא מסתיר content רלוונטי מאחוריו.

---

## כלל 9 — ריווחים שווים ומרכוז עקבי

**הכלל:** רכיבים שנמצאים באותה רמה היררכית (למשל: chips, כפתורים, כרטיסים) — חייבים לחלוק ריווח זהה ביניהם. אין "כמעט שווה".
**מרכוז:** אלמנטים שמוצגים במרכז המסך (כמו flow בחירה) — מרוכזים הן אופקית הן אנכית (או לפחות אופקית), לא נשמטים לצד.
