# Release — Batch fixes (write archive delete, welcome-back, intake reset)

**Date:** 2026-06-03
**Release Manager:** Sam
**Branch:** main → production (Vercel)
**Commit:** `09e8e1f`
**Deployment:** `dpl_9fJFhjmasAnymNx3tZZWqF5eNdda` — READY (production)
**Live:** chat.getbetween.app

## Scope

A batch of three accumulated UX/content fixes in `public/chat.js`. No new feature, no
direction change.

## Issues / changes

| Item | Description | QA |
|------|-------------|----|
| BW-70 | Delete a single entry from the writing archive (× button, confirm, re-render) | Eitan ✅ — id is numeric, type-matched; confirm fires; stopPropagation; RTL/EN correct |
| Welcome-back copy | Removed prior-session clinical summary from "welcome back" — clean greeting only (both code paths) | Eitan ✅ — Aya's direct request (no prior-session material leading into the next session) |
| Intake reset | Restored intake-reset row in settings (modal-cache fix: static row toggled in loadSettingsForm) + copy "אפס" → "להתחיל מחדש" / "Start over" (Shaun) | Eitan ✅ — toggle runs on every modal open |

## Files

- `public/chat.js` — only file in the release (33 insertions, 19 deletions)

## Preflight

- `tsc --noEmit` — PASS (exit 0)
- Local was behind origin/main (5 cron/agent commits: QA, newsletter, daily summary, memory) → `git stash -u` (out-of-scope: memory/bizdev/mockups) → `git pull --rebase` (clean, no chat.js conflict) → push → `git stash pop`
- Excluded from release (out of scope): bizdev presentation, UX/Hold mockup HTMLs, agent memory files, content/writing/review

## Verification (also closed today, pre-batch)

- BW-58 (Processing vs Performing — 4 theorists) → Done. Lia clinical reading PASS.
- BW-60 (Sycophancy Check — 4 theorists + naval/adam) → Done. Lia clinical reading PASS.
  (Both were already live in production since the hold-flow release; this closed the verification debt.)

## Notes

- Process gap to fix (Sam/Hili): add "close Jira + record in release note" to the release
  Definition of Done, so code that ships doesn't sit un-closed in Jira (as BW-58/BW-60 did).
