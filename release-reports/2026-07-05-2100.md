# Release — 2026-07-05 21:00

## גרסה
פיצ'ר ההחזקה במצב מטופל (buckets A+C) — פרודקשן מיושר ללוקאל, למעט מצב מחקר שנשאר נעול-ללוקאל. הוראת איה: "תביא את פרודקשן למצב לוקאל חוץ מפיצ'ר המחקר".

## Commit
`ba5e9fb` — `Patient holding conversation + write-summary voice (buckets A+C)`
- 6 קבצים, 365 insertions / 143 deletions:
  - `app/page.tsx` — זרימת picker תיאורטיקן + החזקה; פריט מחקר נעול ב-`{isLocalhost && ...}`
  - `public/chat.js` — `selectHoldTheorist` / `startHoldConversation` / `enterHoldConversation` + UI ה-picker
  - `app/globals.css` — סגנונות `bw-hold-chip`
  - `app/api/chat/route.ts` — תנאי explore ל-ragContext (no-op בפרודקשן)
  - `lib/rag.ts` — `formatChunksForPrompt(chunks, withCitationTag=true)` — backward-compatible מלא
  - `app/api/write-summary/route.ts` — שכתוב קול ויניקוט בגוף-ראשון

## Scope discipline
commit **מצומצם by-name** — 6 קבצי מוצר בלבד. working tree מלוכלך מאוד (~60 קבצי רעש: docs, bizdev, mockups, agent-memory, eval-reports). **לא בוצע `git add -A`.**

## Pre-flight
- `npm run build` → ✅ נקי, 37/37 עמודים, `Compiled successfully`.
- הרחוק היה even עם local (0/0) — אין cron commits חדשים. `git pull --rebase --autostash` בוצע לזהירות, rebase up-to-date, autostash חזר בלי קונפליקט.
- push: `7ff8351..ba5e9fb main -> main`.

## הבחנה מרכזית — מצב מחקר נעול בקוד עצמו
מצב מחקר (explore) כבר עטוף ב-`{isLocalhost && ...}` ב-`page.tsx` (עם תגית `(לוקאל)`). כלומר העלאת הלוקאל כמו-שהוא **אינה** חושפת מחקר בפרודקשן — `isLocalhost=false` שם, הכפתור לא מרונדר. הבידול שאיה ביקשה מובנה בקוד; לא נדרש ניתוח קבצים ידני.

## Deploy
- Vercel `dpl_gqSQLB8G1mn8grPKrvYy7gQhA7Bb` → **READY** (build 24.5ש', region iad1).
- כל ה-aliases מצביעים לגרסה: `chat.getbetween.app`, `psychoanalytic-space.vercel.app`.
- מועמד rollback: `7ff8351` (`dpl_9jggd7ndPRdf8RV5Sg336eC9y4SN`).

## Post-deploy smoke (חי על פרודקשן)
- `chat.js` — קוד ההחזקה החדש נוכח (10 התאמות `startHoldConversation`/`selectHoldTheorist`/`enterHoldConversation`) ✅
- `chat.js` — הכפתור הישן `"שוחח עם תיאורטיקן"` נעלם (0 התאמות) ✅
- דף ראשי → **200** ✅
- HTML פרודקשן — `sb-explore-btn` **absent** (0) → מצב מחקר לא חשוף ✅
- `POST /api/write-summary` → **401** (route חי, gating תקין, בלי crash) ✅

## מה לא נבדק (מכוון / דגל)
- **`write-summary` — שכתוב קול ויניקוט לא עבר QA מלא.** זה התוכן היחיד החדש שמגיע למטופל בלי סבב QA. ה-route חי (401), אבל איכות הקול עצמה לא אומתה בשיחה חיה. מומלץ smoke קצר של איתן על תוכן write-summary אחרי deploy.
- זרימת ההחזקה עצמה אומתה ברמת "הקוד עלה + build נקי", לא בהרצת שיחה מלאה חיה.

## תוצאה
✅ **הריליס סגור.** פרודקשן מיושר ללוקאל במצב מטופל, מצב מחקר נשאר נעול-ללוקאל כפי שנדרש, אפס חשיפת מחקר בפרודקשן. הדגל היחיד לסגירה: QA תוכן write-summary.
