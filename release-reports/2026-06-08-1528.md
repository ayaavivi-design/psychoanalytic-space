# ריליס — 08.06.2026 15:28

## מה נשלח
commit `001d7c2` — "UX + clinical batch: single header divider, message labels, Klein reality-reason guard"
(push `1c74b51..001d7c2`, אחרי `git pull --rebase` על 7 commits של crons)

## אישורים
- איתן (QA): ✅ אושר לדחיפה. 4/4 פריטים PASS · tsc נקי · lint:tokens נקי. שני WARNING לא-חוסמים (ראה למטה).
- ליה (קליני): ✅ PASS, Tier 2. הכלל של קליין שומר על הקול, לא מקהה.
- איה (מייסדת): ✅ go מפורש ("סם תנהל את הריליס הזה" + "go").
- אדם (CEO): לא נדרש — באץ' UX+קליני קטן, לא שינוי כיוון אסטרטגי.

## מה הולך לפרודקשן
- **פס הדר יחיד** — הוסר ה-border-top מ-.header-session (חזר בטעות אחרי revert; תוקן).
- **תוויות הודעה** — כל הודעת תיאורטיקן מתויגת בשם הספציפי (פרויד/קליין/...), הודעות היוזר בלי תווית.
- **footer pills במסך Hold** — border ו-color עברו ל-muted→text/border לקריאוּת.
- **כפתור "שיחת היכרות" בהדר** — על accent-deep (WCAG 5.63, לבן על accent היה 3.96 fail).
- **כלל קליני לקליין** — כשמטופל נותן סיבה מבוססת-מציאות, להחזיק אותה כממשית לפני שמתרגמים לקנאה/משאלה/פנטזיה; וכשמדובר במטפל האמיתי — להחזיר לשם, לא לקחת את ההעברה על עצמך.
- סנכרון דוקומנטציה (design-system).

## WARNING לא-חוסמים (איתן)
1. שני תיאורטיקנים פעילים בלי attribution → נופל ל-"Agent" גנרי (edge case של השוואת תיאורטיקנים, לא regression).
2. revert של welcome-spans "לא בעץ העבודה" — תקין וצפוי: ה-spans כבר על accent-dim שזה ערך HEAD, אז אין diff.

## דיפלוימנט
- `dpl_Hsr7NXvUCu3jYMuHnNKwqgmZsbgv` → READY (build ~24s) · iad1 · חי על chat.getbetween.app

## הבא: QA פרודקשן
איתן — גרסה בפרודקשן. מחכה לאישורך.
