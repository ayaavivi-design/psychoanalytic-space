# Release — 2026-07-05 18:52

## גרסה
BW-124 — מד-האמת האנליטי (analytical fidelity meter). דלי B בלבד, אדיטיבי, מוגן ב-`QA_SECRET`, on-demand בלבד (לא בקרון). אפס user impact.

## Commit
`c6bbad2` — `BW-124: analytical fidelity meter (bucket B, additive)`
- 5 קבצים חדשים, 666 שורות, כולם additive:
  - `app/api/fidelity/route.ts`
  - `lib/rubric-prompt.ts`
  - `lib/fidelity-fixtures.ts`
  - `lib/fidelity-scenarios.ts`
  - `docs/FIDELITY-RUBRIC.md`

## Scope discipline
commit **מצומצם by-name** בלבד — 5 קבצי דלי B. working tree היה מלוכלך מאוד (~60 קבצי רעש + דליים A/C לא ב-scope). לא בוצע `git add -A`. דליים A (rag.ts, chat/route.ts), C (page.tsx, chat.js, globals.css) ו-D (רעש) נשארו uncommitted.

## Pre-flight
- הרחוק התקדם 10 cron commits בזמן העבודה (`5066f8b` → `b062c88`). בוצע `git pull --rebase --autostash` — rebase נקי, autostash חזר בלי קונפליקט, ה-working tree המלוכלך נשמר כמו שהיה.
- push: `b062c88..c6bbad2 main -> main`.

## Approvals
- **איתן (QA smoke):** ✅ `release/eitan-approval.md` — build נקי, `/api/fidelity` ב-route manifest, אפס-רגרסיה (5 קבצים untracked, אפס import קיים), gating code-review.
- **איה:** ✅ go ("ממשיכים").
- **אדם:** לא נדרש (כלי פנימי, אפס user impact).

## Deploy
- Vercel `dpl_2N4R3dW23DzHW2chaiUUXAtWensF` → **READY** (build ~25 שניות, region iad1).
- כל ה-aliases מצביעים לגרסה: `psychoanalytic-space.vercel.app`, `chat.getbetween.app`.
- מועמד rollback אם צריך: `b062c88` (`dpl_CAogAoTdpASRh88fs3r9u2yDTkND`).

## Post-deploy smoke (eitan-prod — gating חי)
אימות runtime של ה-gating (הקביעה שנשארה code-review-בלבד ב-QA):
- `GET /api/fidelity` בלי secret → **401** ✅
- `?secret=wrong` → **401** ✅
- `?secret=<valid>` בלי theorist → **400** (עבר gating, נפל על validation) ✅ — מוכיח שה-secret עובר, בלי קריאת LLM (אפס עלות).

## מה לא נבדק (מכוון)
- **baseline מלא** (4 קולות generate-then-score + test-retest) — עולה כסף, אישור נפרד של איה. לא בריליס הזה. הדגל של איתן: ציון בודד = שיפוט-LLM יחיד, בלי שונוּת לא נדע אם שינוי הוא אות או רעש.

## תוצאה
✅ **הריליס סגור ויציב.** מד-האמת חי בפרודקשן, gating מאומת runtime, אפס רגרסיה לבוטים/chat/UI.
