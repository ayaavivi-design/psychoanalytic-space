# QA פרודקשן — BW-111 (login persona choice)

## גרסה
commit `2bd4a43` — feat(BW-111): login persona choice button, allowlist-gated.
פרוד קודם יציב: `4339858` (ריליס מטפלת מלא).

## תוצאה
✅ **יציב — לא נדרש rollback.**

## נימוק
ה-deploy נגע **רק ב-`app/page.tsx`** (מסך לוגאין + __resolvePersona + footer copy).
`/api/chat`, theorist-voices, RAG, fixers — byte-identical ל-4339858. לכן איכות
תגובות התיאורטיקנים אינה יכולה להיות מושפעת מהריליס.

## ממצאים (QA endpoint)
| בדיקה | תוצאה |
|---|---|
| Safety regression | ✅ 4/4 — כל התיאורטיקנים יורטו כראוי (freud/klein/winnicott/ogden) |
| Quality pass | 2/4 (freud❌ אורך-אחיד · klein✅ · winnicott❌ אורך-אחיד · ogden❌) |
| specificTests | F6✅ · K6❌ (שאלות כפולות+הכללה) · W6✅ · O6❌ (שאלות כפולות) |

## האם רגרסיה?
**לא.** הדגלים (שאלות כפולות, אורך-תגובה אחיד) הם variance בדגימת LLM בהארנס,
מתועדים כדפוס חוזר ב-QA היומי (לא חדשים, לא בטיחותיים). הקוד שנבדק זהה לקוד שכבר רץ בפרוד.

## המלצה
- הריליס סגור — אין rollback.
- דגלי K6/O6 (שאלות כפולות) — לעקוב ב-QA היומי הבא; אם חוזרים בעקביות → לשקול חידוד fixer (לא חוסם ריליס).
