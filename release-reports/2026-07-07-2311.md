# Release — 2026-07-07 23:11

## גרסה
CORE_GUARDRAILS גל 1 (Wave 1) — בלוק גארדרילז אוניברסלי ברמת-פרומפט, נכתב פעם אחת ומוזרק לכל 4 הקולות (פרויד/קליין/ויניקוט/אוגדן) דרך `staticSystem`. שלושה צירים: G8 (NO COLLUSION) · G9 (NO FABRICATION) · G11 (OVERRIDE RESISTANCE). כולל גם חיזוק ה-anti-scripting של Hold-B ב-theorist-voices.

## Commit
`ad07ce7` — `Add CORE_GUARDRAILS block (Wave 1: G8/G9/G11) + Hold-B anti-scripting`
- 2 קבצים, 43 insertions / 0 deletions (אדיטיבי טהור — אפס מחיקות):
  - `lib/theorist-voices.ts` — export חדש `CORE_GUARDRAILS` (~שורה 2971, אחרי SAFETY_PROTOCOL) + חיזוק Hold=contain anti-scripting (~שורה 1088)
  - `app/api/chat/route.ts` — import של `CORE_GUARDRAILS` (שורה 5) + הצמדה ל-`staticSystem` (~שורה 418)

## הבחנה מרכזית — למה staticSystem ולא dynamicSystem
הבלוק הוצמד ל-`staticSystem` **בכוונה**: `UNIVERSAL_SCOPE_INSTRUCTION` (רובד II הישן) מוזרק ב-dynamicSystem רק כשה-RAG מחזיר ריק (route.ts:471) — וכשה-RAG מצליח (המצב הרגיל ל-4 הקולות) הבלוק האוניברסלי הישן נופל. staticSystem מבטיח נוכחות בכל תור, בכל מסלול (RAG מצליח/ריק/נכשל), ונשאר cached.

## Scope discipline
commit **מצומצם by-name** — 2 קבצים בלבד, שניהם guardrails-only. working tree מלוכלך (write-summary held-fallback + sidebar sync + כפתור "לנתח") — **הוחזק בחוץ מכוון**: write-summary לא עבר QA חי, "לנתח" gated על החלטת מוצר של הילי (BW-122). לא בוצע `git add -A`.

## Pre-flight
- `npm run build` → ✅ נקי, 37/37 עמודים, `Compiled successfully`.
- `git pull --rebase --autostash` — הרחוק היה מקדים (Vercel crons), rebase נקי, autostash חזר בלי קונפליקט. local SHA לפני rebase `7e90917` → אחרי `ad07ce7`.
- push: `ea5b599..ad07ce7 main -> main`.

## Deploy
- Vercel `dpl_FnRa2JNDV6nUCwxxpU5YgiS87PN5` (commit ad07ce7, target production) → **READY** (build ~26ש', region iad1).
- כל ה-aliases מצביעים לגרסה: `chat.getbetween.app`, `psychoanalytic-space.vercel.app`.
- מועמד rollback: `ea5b599`.

## QA לפני שחרור
- **איתן harness ישיר** (node type-stripping, ייבוא `THEORIST_VOICE` + `CORE_GUARDRAILS` אמיתיים, קריאה ישירה ל-Claude): 8 תרחישים × 4 קולות = **60/60 נקי, אפס הפרה**. G8/G9/G11 — כולם PASS.
- caveat מתועד: ה-harness עוקף את לולאת ה-validation ואת ה-RAG — בודק ציות ברמת-המודל לפרומפט בלבד. `/api/chat` נעול-JWT (401 ל-curl ישיר), לכן אין הרצת endpoint חי מול auth. לא הומצאו תוצאות.

## דגלים (לא חוסמים)
- 2 הערות קוד סטיליות ישנות: `route.ts:418` ו-header של theorist-voices עדיין כתובות "(G9 + G11)" במקום G8+G9+G11. קוסמטי בלבד — לאוליבר בקומיט נפרד. **לא נגעתי בהן לפני push כדי לשחרר בדיוק את מה שעבר QA.**

## תוצאה
✅ **פרודקשן עלה — READY.** ממתין ל-QA פרודקשן של איתן לסגירת הריליס.
