⚠️ ACTION REQUIRED

# Dependency Health — 2026-06-08

## Vulnerabilities (12 total)
| Severity | Count |
|----------|-------|
| Critical | 1 |
| High     | 4 |
| Moderate | 7 |
| Low      | 0 |

## Packages Requiring Action

| Package | Current | Fix | Issue |
|---------|---------|-----|-------|
| `next` | 16.2.1 | 16.2.7 | HIGH — DoS (Server Components) + middleware/proxy bypass |
| `@anthropic-ai/sdk` | 0.80.0 | 0.102.0 | MODERATE — memory tool path traversal + insecure file permissions |
| `@xenova/transformers` | 2.17.2 | — | CRITICAL (onnxruntime-web) + HIGH (protobufjs) — no clean fix; transitive chain |
| `resend` | 6.12.4 | patch | MODERATE — uuid bounds check via svix |

## Action Required: YES

1. **Run now:** `npm audit fix` — fixes `next`, `resend`, `ws` automatically
2. **Manual:** Upgrade `@anthropic-ai/sdk` to `^0.102.0` (major bump — test API compatibility)
3. **Watch:** `@xenova/transformers` has no safe upgrade path yet; monitor upstream for a release that drops the vulnerable `onnxruntime-web` version
