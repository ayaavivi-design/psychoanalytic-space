# Dependency Health — 2026-06-15

## Vulnerabilities (3 moderate, 0 high, 0 critical)

| Package | Severity | Issue |
|---|---|---|
| `@anthropic-ai/sdk` 0.79–0.91 | moderate | Memory Tool path traversal + insecure file permissions (GHSA-5474-4w2j-mq4c, GHSA-p7fg-763f-g4gf) |
| `postcss` <8.5.10 (bundled in `next`) | moderate | XSS via unescaped `</style>` in CSS output (GHSA-qx2v-qp2m-jg93, CVSS 6.1) |

## Outdated packages that matter

| Package | In use | Latest | Notes |
|---|---|---|---|
| `@anthropic-ai/sdk` | ~0.80 | 0.104.1 | Major drift; upgrade also fixes both audit findings |
| `react` / `react-dom` | 19.2.4 | 19.2.7 | Patch; low priority |

## Action required: No (no critical/high)

**Recommended:** Upgrade `@anthropic-ai/sdk` to ≥0.91.1 (fixes both CVEs). This is a major bump — test before deploying.

**Note on postcss:** `npm audit --fix` suggests downgrading `next` to 9.3.3 — do NOT do this. The real fix is adding `"overrides": { "postcss": ">=8.5.10" }` to `package.json` and re-locking. Low urgency since this is a build-time CSS tool, not runtime user-facing code.
