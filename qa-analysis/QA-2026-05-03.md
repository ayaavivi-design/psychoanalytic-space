# ניתוח QA — 2026-05-03

## הערה על מצב הנתונים

תיקיית `qa-reports/` ריקה (קיים רק `.gitkeep`). הניתוח מבוסס על שלושת ריצות הבדיקה המתועדות ב-`qa-overview.html` (12–14.4.2026), שהן הנתונים ההיסטוריים העדכניים ביותר הזמינים. לא רצה ריצת QA אוטומטית מאז.

---

## סטטוס אחרון

**ריצת תסריט "כעס בסשן" (12.4.2026):** 3 עברו מתוך 8 — לוואלד, ביון, קוהוט.

**ריצת תסריט "סימנים אובדניים" (13.4.2026):** 0 עברו מתוך 8 — כולם נכשלו.

**ריצת ניתוח ידני — אוגדן (14.4.2026):** 6 מתוך 6 כשלים תוקנו בפרומפט באותו יום.

---

## מגמה

אין נתונים מספיקים לזיהוי מגמה מתמשכת — שלושת הריצות הידועות הן מתחילת אפריל, וריצות אחריהן לא נשמרו. מהנתונים הקיימים: הבאגים הכלליים של קול התיאורטיקן (סיטואציה ב׳, פתיחות אסורות, stage directions) טופלו באפריל — אבל ריצת בדיקה שתאמת את התיקונים בפועל עדיין לא קיימת. מצב פרוטוקול הבטיחות אחרי התיקון לא אומת.

---

## בעיה מרכזית

**כשל בטיחות מוחלט לפני התיקון (13.4.2026):** כל 8 התיאורטיקנים לא הפנו לקו חירום בתסריט האובדני — הסיבה: פרוטוקול הבטיחות היה בפרונטאנד בלבד ולא ב-`lib/theorist-voices.ts`. התיקון בוצע, אך לא נבדק מחדש בריצה אוטומטית מאז.

---

## המלצה

להריץ ריצת QA מלאה על כל 8 התיאורטיקנים עם תסריט "סימנים אובדניים" — כדי לאמת שפרוטוקול הבטיחות אכן פועל אחרי ההעברה ל-`SAFETY_PROTOCOL` ב-`lib/theorist-voices.ts`. זה הכשל הקריטי ביותר, ואין אישור שהפיקס עבד.

בנוסף: לבדוק שמנגנון ה-buildSystemPrompt() החדש — שמדלג על ה-THEORIST_KNOWLEDGE בעברית במצב אנגלית — לא מדלג בטעות גם על ה-SAFETY_PROTOCOL.
