# ניתוח QA — 22 באוגוסט 2026

## סטטוס אחרון
4/4 עברו — פרויד, קליין, ויניקוט, אוגדן. אין כשלים ב-5 דוחות QA האחרונים.

## מגמה
יציבות מלאה בדוחות האוטומטיים — 4/4 נקי מאז 7 באוגוסט. אין תיאורטיקן שחוזר ונכשל בבדיקות האוטומטיות. **אבל: ה-QA האוטומטי מכסה 4 מתוך 8 תיאורטיקנים בלבד** — לוואלד, ביון, קוהוט, והיימן אינם נבדקים כלל, ומדדי ה-Judge (שונים מה-QA) מגלים שויניקוט נכשל שוב ושוב בבדיקות עמוקות יותר.

## בעיה מרכזית
**לולאה פתוחה #8 חוסמת כל מסקנה על תקינות הפרומפטים.**

ה-OPEN_LOOPS מתאר שגיאה ב-`app/api/chat/route.ts:284` — `body.system` נזרק לפני שמגיע למודל. אם זה נכון, כל תיקוני ה-8c שנוספו ל-`lib/theorist-voices.ts` (שורה 1416, נוסף אחרי 16.08) **מעולם לא הגיעו למודל**. זה מסביר את תעלומת ויניקוט: הכלל קיים בקוד — ועדיין D-1/D-2 מופיעים ב-4 דוחות Judge עצמאיים (25.07, 01.08, 16.08, 17.08). לא בגלל שהניסוח לא מספיק — אלא כי הניסוח לא מגיע.

ה-QA האוטומטי מראה "נקי" כי הוא בודק תוצר, לא נתיב. גם תוצר שמגיע ממודל ללא system prompt יכול לעבור בדיקת פורמט בסיסית.

## המלצה
**לפני כל תיקון נוסף בפרומפטים — לאמת את נתיב `body.system` ב-`route.ts:284`.**

צעד ספציפי: הוסף `console.log('system prompt received:', !!body.system, body.system?.slice(0,100))` לשורה 283 ב-`app/api/chat/route.ts`, דפלוי, שלח שיחת ויניקוט, בדוק את ה-Vercel logs. אם הלוג מראה `false` או `undefined` — כל עבודת ה-8c הייתה על מערכת מנותקת, ויש לעבור את `buildSystemPrompt()` לצד השרת. אם הלוג מראה `true` — הבעיה היא בניסוח 8c ויש ליישם את תיקון ליה (שורה 1416).

---

**CONFIDENCE LEVEL:**
WARNING — דורש אימות ידני, לא FAIL.

הדוחות האוטומטיים נקיים ואמינים לגבי מה שהם בודקים (4 תיאורטיקנים, בדיקת פורמט). האבחנה על `body.system` מבוססת על קריאת OPEN_LOOPS בלבד — טרם אומתה בקוד חי. עד לאימות, כל מסקנה על תקינות הפרומפטים היא ספקולטיבית.
