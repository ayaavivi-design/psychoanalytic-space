You are running **/handover** — a end-of-session filter. איה מריצה אותך בסוף יום עבודה.

**המטרה:** לסנן באגרסיביות. רוב הסשן הוא execution שנשכח; ה-5% שנושאים משקל לפעם הבאה — זה מה ששומרים. הפתק הסופי חייב להיקרא בפחות מ-30 שניות.

Respond in Hebrew. מונחים באנגלית (commit, deploy, in-flight) בסוגריים צמוד למילה העברית. אל תתחיל שורה/כותרת במילה אנגלית.

---

## PHASE 1 — סקור מה באמת קרה (read-only)

הרץ במקביל:
- `git status` — שינויים לא-committed (staged + unstaged + untracked)
- `git log origin/main..HEAD --oneline` — commits מקומיים שעוד לא נדחפו
- `git log --since="14 hours ago" --oneline --author-date-order` — מה נגע בסשן הזה (סנן החוצה commits של בוטים: QA-Bot, Summary Bot, Lia Judge, Adam CEO, Naval, Shaun — אלא אם הם רלוונטיים)
- אם `~/.claude/PENDING_TASKS.md` קיים — קרא אותו כדי לדעת מה היה פתוח בכניסה לסשן

**אל תדווח על כל שורה.** אתה אוסף חומר גלם — הסינון קורה ב-PHASE 2.

---

## PHASE 2 — מה נושא משקל (הלב)

מכל מה שאספת, שמור **רק** את מה שקורא-קר בסשן הבא יצטרך:
- **החלטות שהתקבלו** — ולמה (הנימוק, לא רק המסקנה)
- **אילוצים שהתגלו** — משהו שלא ידעת בהתחלה וכובל את ההמשך
- **חצי-גמור** — מה נפתח ולא נסגר, ומה הצעד הבא המדויק
- **footguns** — מלכודות שנתקלת בהן (למשל: commit שסחף קבצים pre-staged; env var חסר; endpoint רדום)

**זרוק החוצה:** ניסוחים, ניסיונות שהוחלפו, קריאות שהצליחו בלי הפתעה, כל דבר שנקרא מהקוד/git ב-10 שניות. אם זה לא ישנה החלטה עתידית — זה לא נכנס.

---

## PHASE 3 — עדכן זיכרון (מסונן, לא לוג יומי)

- **`~/.claude/PENDING_TASKS.md`** — זה הקובץ שנועד ל"מה נשאר". עדכן אותו: הוסף פריטים חצי-גמורים + watch-outs, הסר פריטים שנסגרו היום. תאריכים יחסיים → תאריכים מוחלטים. **אל תכתוב פעילות יומית — רק מה שפתוח קדימה.**
- **`MEMORY.md` / `OPEN_DECISIONS.md` / `STRATEGIC_PRIORITIES.md`** (אם קיימים בפרויקט) — אלה קבצים כבדים. **אל תכתוב אליהם ישירות.** אם משהו בסשן משנה אותם (החלטה נסגרה, אילוץ חדש) — **הצע** את השינוי המדויק בצ'אט ובקש אישור לפני כתיבה.
- **auto-memory** (`~/.claude/projects/.../memory/`) — אם עלה feedback/preference חדש של איה, שמור לפי הכללים הרגילים. אחרת דלג.

---

## PHASE 4 — commit? (רק בשאלה)

אם יש שינויים לא-committed ששווה לשמר:
- הצג אותם בשורה-שתיים (איזה קבצים, מה השינוי)
- **שאל לפני שאתה עושה commit.** אל תדחוף אף פעם בלי בקשה מפורשת (כלל פרודקשן של איה).
- ⚠ אם הריפו כבר מכיל קבצים pre-staged שלא קשורים — הזהר, כי `git commit` יבלע אותם. הצע commit של קבצים ספציפיים בשם.

---

## PHASE 5 — הדפס את פתק ההעברה (תמיד, בצ'אט)

ארבעה סקשנים בלבד. קצר. כל שורה חייבת להצדיק את מקומה:

```
━━ העברה — [תאריך] ━━

מה נחת (shipped)
[commits שנדחפו + deploys. אם כלום — "כלום נדחף". שורה לכל פריט.]

מה עדיין באוויר (in-flight)
[חצי-גמור, עם מספיק הקשר לקורא-קר: מה, איפה עצרנו, הצעד הבא. לא יותר מ-3-4 פריטים.]

להיזהר מ (watch-outs)
[מלכודות, state מפתיע, הנחות שנשברו. אם אין — דלג על הסקשן.]

שאלות פתוחות אלייך
[מה שדורש החלטה של איה לפני שממשיכים. אם אין — דלג.]
```

---

## גבולות
- **אל תדחוף כלום** ואל תעשה commit בלי אישור מפורש.
- **אל תיגע בפרודקשן.**
- אם הסשן היה כולו execution בלי שום דבר נושא-משקל — אמור זאת בכנות ("סשן שקט, אין מה להעביר") במקום להמציא פריטים.
- קצר > מלא. אם יש ספק אם פריט נכנס — הוא כנראה לא.
