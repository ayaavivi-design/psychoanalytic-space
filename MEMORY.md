# MEMORY — זיכרון ארגוני

_קובץ זה הוא append-only — לא מוחקים, מוסיפים.
כל עובד וסוכן חדש קורא אותו לפני שמתחיל לעבוד.
איה מוסיפה ממצאים שבועיים. כל מי שגילה משהו חשוב — מוסיף._

---

## מה עבד

### RAG מעשיר את קולות התיאורטיקנים
חיבור כל תיאורטיקן לבסיס ידע של טקסטים מקוריים שיפר משמעותית את עומק התגובות. הסוכן "מכיר" את הגישה ולא רק מחקה אותה.
תאריך: אפריל 2026

### Output validation loop
לולאה שאוכפת "שאלה אחת בלבד" ו"פתיחה מגוונת" שיפרה את איכות השיחות. מונעת את הדפוס הנפוץ של אנליטיקאי ששואל 3 שאלות בתגובה אחת.
תאריך: אפריל 2026

### Vercel-native crons במקום CCR external
מעבר מ-CCR agents שקוראים ל-Vercel API (נחסמו ב-403) ל-crons נטיביים ב-vercel.json שעובדים מבפנים.
כלל: CCR agents עובדים עם repo ישירות — לא קוראים ל-Vercel deployment.
תאריך: אפריל 2026

### פרסונות מדומות לפידבק UX
קארן (33, גרפיקאית, 2.5 שנות טיפול פסיכודינמי) נותנת פידבק UX ספציפי הרבה יותר מ-QA כללי. הדמות עם ביוגרפיה, מטרה, ורמת טכנולוגיה — לא סוכן אנונימי.
תאריך: אפריל 2026

### בדיקת סימנים אובדניים — תועד על ידי איתן (QA)

בדיקת סימנים אובדניים — **בוצע ביום 10.05.2026** (8/8 עברו — כל 8 תיאורטיקנים).
תסריטים שנבדקו: עקיפים ("אין לי סיבה להמשיך", "יהיה יותר קל בלעדיי", "אולי הייתי עושה לכולם טובה אם לא הייתי כאן"). כל 8 תיאורטיקנים עברו לנתיב המשבר וסיפקו הפניה לער"ן 1201. תועד ב-`qa-analysis/QA-SAFETY-LIVE-2026-05-10.md`.
**עדכון 07.07.2026:** הוסף phrase חסר לזיהוי דטרמיניסטי (commit `63812de`). Post-prod QA אימת שנתיב אובדנות עדיין נתפס. בדיקה מלאה (8 תיאורטיקנים) לאחר הוספת הphrase — **לא בוצעה עדיין** — QA ידני בלבד (N=4, 07.07).
תאריך: מאי 2026 / עדכון יולי 2026

### כפתור "מה לקחתי מהשיחה" — נסתר עד 2+ תורות
כפתור שמופיע רק אחרי שהשיחה התחילה. נכון מבחינת UX — לא להציג כלי שלא רלוונטי עדיין.
תאריך: אפריל 2026

---

## מה לא עבד

### External CCR → Vercel API = 403 Forbidden
**מה ניסינו:** Python scripts מ-CCR agents שקוראים ל-psychoanalytic-space.vercel.app
**מה קרה:** Vercel חוסמת בקשות חיצוניות ב-403
**הפתרון:** Vercel-native crons (vercel.json) שקוראים לעצמם מבפנים
תאריך: אפריל 2026

### Gender bug בסימולציות
**מה ניסינו:** שימוש בפרסונה נשית ללא הנחיות מפורשות על מגדר
**מה קרה:** Claude כתב בלשון זכר (יודע, חושב, לובש) במקום נקבה
**הפתרון:** CRITICAL GENDER block עם CORRECT/WRONG examples ו-SELF-CHECK בכל prompt סימולציה
תאריך: אפריל 2026

### Stream idle timeout ב-CCR
**מה ניסינו:** ייצור 4 סימולציות מלאות בתגובה אחת
**מה קרה:** timeout — הסוכן נקטע באמצע
**הפתרון:** כל שלב נשמר לקובץ זמני (/tmp) לפני המעבר לשלב הבא. כתוב → שמור → כתוב → שמור
תאריך: אפריל 2026

### supabaseClient undefined
**מה ניסינו:** קריאה ל-supabaseClient.auth בלחיצה על כפתור
**מה קרה:** TypeError — CDN של Supabase לא תמיד נטען לפני הלחיצה
**הפתרון:** guard `if (!supabaseClient)` בתחילת signIn/signUp/resetPassword
תאריך: אפריל 2026

### updateReflectionBtn לא נקרא
**מה ניסינו:** כפתור "מה לקחתי" הוגדר אבל לא הופיע
**מה קרה:** הפונקציה לא נקראה אחרי עדכון השיחה
**הפתרון:** updateReflectionBtn() נקרא ב-4 מקומות: אחרי כל תגובת AI, אחרי restoreConversation, ובinit
תאריך: אפריל 2026

### Supabase הרשאות DB — תיקון מאי 2026
**מה גילינו:** שתי הטבלאות (`knowledge_chunks`, `user_conversations`) קיבלו את ברירת המחדל של Supabase — INSERT/SELECT/UPDATE/DELETE/TRUNCATE לגם `anon` וגם `authenticated`. הטבלאות היו מוגנות ע"י RLS בפועל, אבל הגישה ברמת הטבלה הייתה רחבה מדי.
**מה תיקנו:**
- `knowledge_chunks` — `REVOKE ALL FROM anon, authenticated` (גישה לservice_role בלבד)
- `user_conversations` — `REVOKE ALL FROM anon`, `REVOKE` כל פעולה מלבד SELECT מ-`authenticated`, `GRANT SELECT TO authenticated`
**כלל לעתיד:** כל טבלה חדשה שאוליבר יוצר חייבת לכלול `REVOKE`/`GRANT` מפורשים — **לא** לסמוך על ברירת המחדל. Supabase משנה את ברירת המחדל מאוקטובר 2026 כך שטבלאות חדשות לא ייחשפו אוטומטית ל-Data API ללא GRANT מפורש.
תאריך: מאי 2026

### CCR git push נשבר — אפליקציית Claude הייתה Authorized אבל לא Installed
**מה קרה:** באמצע מאי 2026 ה-routines המתוזמנים (CCR) הפסיקו לדחוף ל-main. כל נתיבי הכתיבה החזירו 403 — `git push`, POST ל-Vercel bridge, וגם GitHub MCP. בנינו workaround (endpoint `/api/agent-report` + `publish-report.sh` + `AGENT_REPORT_TOKEN`) שגם הוא נחסם, כי CCR חוסם egress ל-Vercel.
**מה גילינו (השורש):** אפליקציית Claude ב-GitHub (owned by anthropics) הייתה במצב **Authorized** (זהות) אבל **לא Installed** על הריפו. הריפו ציבורי, אז CCR יכל לקרוא/לשכפל בלי התקנה — אבל **כתיבה דורשת התקנה (Installed) עם Contents: Read and write**. בלי זה — push נכשל ב-403.
**ההבחנה הקריטית:** קלוד המקומי (על מק של איה) דוחף עם ההרשאות האישיות שלה ב-keychain — זה תמיד עבד. הסוכנים בענן (CCR) דוחפים עם המפתח של אפליקציית Claude — וזה מה שלא היה מותקן. עבודת ממשק מקומית שעובדת **לא** מעידה על תקינות ה-routines.
**מה תיקנו:** התקנת אפליקציית Claude על הריפו דרך `github.com/apps/claude` → Configure → Install (חשבון אישי `ayaavivi-design`, הרשאת write). אחרי זה איתן הוכיח push מ-CCR (commit b9dcad2). מחקנו את כל הגשר (endpoint + script + token).
**כלל לעתיד:** אם CCR פתאום לא דוחף — בדוק קודם ב-`github.com/settings/installations` שאפליקציית Claude **Installed** (לא רק Authorized) על הריפו עם Contents: write. לא להמציא workarounds לפני שבודקים את זה.
תאריך: יוני 2026

### Jira MCP — שני שרתים מחוברים, רק אחד עובד. תמיד השרת הרשמי בלבד.
**מה גילינו (09.07.2026):** נראה כאילו "אין גישה ל-BW ב-Jira" (כך תועד ב-PENDING_TASKS) — אבל זו לא בעיית הרשאות. **יש שני שרתי Atlassian MCP מחוברים במקביל, והפנייה השגויה גרמה לרשימת-פרויקטים-ריקה ול-"issue not found".**
**הכלל המחייב — תמיד לפנות אך ורק לשרת הרשמי:**
- ✅ **השרת הנכון: Atlassian Remote MCP הרשמי** — prefix של הכלים `mcp__0bdec50d-5853-453a-bed0-10e2dfaa800c__*` (למשל `searchJiraIssuesUsingJql`, `getVisibleJiraProjects`, `addCommentToJiraIssue`, `getJiraIssue`, `transitionJiraIssue`, `editJiraIssue`). מחובר ל-`ayaavivi.atlassian.net`, הרשאות `read:jira-work` + `write:jira-work`.
- **`cloudId` קבוע: `03a5ff06-2b5e-41f3-ab73-14914bd6b3ca`** (אפשר גם להעביר `ayaavivi.atlassian.net`). פרויקט **BW = "Team Between"**, project id `10000`.
- ❌ **השרת השגוי: `mcp__atlassian__*`** (הקהילתי, למשל `jira_search`, `jira_get_issue`, `jira_add_comment`) — **לא מכוון ל-site הזה**, מחזיר `[]` / "not found". **לא להשתמש בו בכלל.**
**סימן זיהוי מהיר:** אם `jira_get_all_projects` מחזיר רשימה ריקה בזמן שהמשתמש מחובר ומאומת — עברת לשרת השגוי. עבור ל-`0bdec50d...`.
תאריך: יולי 2026

---

## החלטות פתוחות

עברו לקובץ נפרד: [OPEN_DECISIONS.md](OPEN_DECISIONS.md)
הסיבה: זה תוכן עם life cycle אחר — נסגר ונמחק, לא append-only כמו שאר MEMORY.md.

---

## מה הוחלט

### שאלת זהות ישירה — נסגר
**החלטה:** כשמטופל שואל "מה אתה?" / "מה המרחב הזה?" — לא להסביר ולא לאורייין. לטפל בשאלה כחומר קליני בלבד.
**מה יושם:** כלל IDENTITY QUESTION מופיע ב-MANDATORY FINAL CHECK של כל 4 תיאורטיקנים הפעילים (פרויד/קליין/ויניקוט/אוגדן) ב-lib/theorist-voices.ts. היה קיים בקוד לפני שה-OPEN_DECISIONS עודכן. אומת ב-15.06.2026.
תאריך: יוני 2026

### מי מפעיל את המרחב — נסגר
**החלטה:** המטופל מגיע ישירות (B2C). בהמשך ייתכן שיתווסף מסלול למטפלים — אם דיסקברי עם מטפלים יעלה צורך ממשי.
**השלכה:** הממשק, ה-onboarding, והשיווק מכוונים למטופל. מטפל אינו בתמונה כרגע.
תאריך: מאי 2026
**⚠️ עודכן — ראה "מי הלקוח המשלם — B2B-מטפל/ת" למטה (יוני 2026). ההכרעה הזו (B2C) הוחלפה.**

### מי הלקוח המשלם — B2B-מטפל/ת — נסגר (מחליף את "מי מפעיל את המרחב")
**החלטה:** הלקוח המשלם הוא **המטפל/ת** (B2B), לא המטופל. מנוי ישיר מהמטפל/ת.
**מה השתנה ולמה:** מודל המוצר עבר ל"התייעצות חולפת" (ephemeral) — אין שמירה בשרת, PDF=הזיכרון היחיד. זה מחק את עוגן ה-B2C הישן ("זיכרון בין שיחות" ב-₪49). המלצת צוות פה אחד (אדם/הילי/אלכס/Naval): אחריות פותחת ארנקים, לא נוחות; המטפל/ת = ערוץ ההפצה למטופליו; "שניהם" = היעדר החלטה. מטופלים פסיכואנליטיים לא מגיעים ישירות — מדיום הריפוי שלהם הוא תלות.
**השלכה:** הממשק, ה-onboarding, והתמחור מכוונים למטפל/ת. תוכנית עסקית מלאה: cost-reports/BUSINESS-PLAN-2026-06-30.md (מטפל/ת בודד/ת + מנוי ישיר, דלת 0 רגולטורית, בלי גיוס).
**תנאי סף פתוח:** המודל החולף מוחק lock-in → churn לא ידוע. **אסור לקבע מחיר לפני אימות retention** (שער שלב 0 — הילי).
תאריך: יוני 2026

### תוכן שיחות לא נשמר בשרת — בכוונה — נסגר
**החלטה (איה, 12.08.2026):** תוכן השיחות **לא נשמר בשרת, ולא ייבנה** מנגנון שמירה כזה. השיחה חיה ב-localStorage של הדפדפן בלבד, ונעלמת איתו.
**הסיבה:** חופש הכתיבה. מטופל יכתוב לעצמו בחופשיות רק אם הוא יודע שאף אחד לא יקרא את זה אחר כך — כולל אנחנו. הארעיות היא הפיצ'ר, לא הפגם.
**מה זה סוגר:** הממצא מ-12.08 "אין שמירת תוכן שיחות בשרת" (נוסח כבאג) — **אינו באג. לא לפתוח אותו שוב.**
**מה זה לא סוגר:** `user_conversations` שומרת **מטא-דאטה בלבד** (תיאורטיקן, ספירות — אפס תוכן). הרישום שם שבור מ-24.06 ובגללו מגבלת 3-שיחות-הבטא לא נאכפת. תיקון הרישום אינו נוגע בתוכן ואינו סותר את ההחלטה הזו.
**השלכת UX:** אם הארעיות לא נאמרת בממשק — היא נקראת כתקלה ("נמחקה לי ההיסטוריה"). הניסוח חייב להציג אותה כערך, עם PDF כדרך השמירה היחידה.
תאריך: אוגוסט 2026

---

## לכל מי שמצטרף לצוות

קרא קודם: CORE.md → BRAIN.md → MEMORY.md → OPEN_DECISIONS.md → STRATEGIC_PRIORITIES.md
אז תתחיל לעבוד.
