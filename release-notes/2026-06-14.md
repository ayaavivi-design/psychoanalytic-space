# ריליס נוט — security(deps) dependency remediation | 2026-06-14

## מה יצא היום לפרודקשן
תיקון אבטחה בתלויות (dependencies) — צמצום פגיעויות npm מ-12 ל-3, כולל מחיקת ה-CRITICAL היחיד. שינוי תשתיתי בלבד: `package.json` + `package-lock.json`. אפס שינוי קוד אפליקטיבי, אפס שינוי בהתנהגות המוצר למשתמש.

## מה עשינו
- **`npm audit fix`** non-breaking — תוקנו uuid + ws (transitive)
- **הסרת `@xenova/transformers`** — חבילה שלא הייתה בשימוש (אפס imports). מחקה את ה-CRITICAL (protobufjs RCE) ומספר High שתלו בה. ה-embeddings רצים ממילא דרך HuggingFace remote API ב-`lib/rag.ts`
- **next `16.2.1` → `16.2.9`** (patch) — מחק 9 advisories של next (middleware bypass, XSS, cache poisoning, SSRF, DoS)

## מה לא יצא
3 פגיעויות moderate שנשארו אינן בנות-תיקון בטוח כרגע, ושתיהן לא חשופות:
- `@anthropic-ai/sdk` Memory Tool (משתמשים ב-Messages API בלבד — לא חשוף)
- `postcss` (transitive בתוך next; ה"תיקון" של npm הוא הורדת next ל-9.3.3 — נדחה עד ש-next יבמפו אצלם)

## אישורים
- איתן (QA): ✅ smoke test PASS (tsc/build EXIT=0, lockfile נקי, e2e פרויד HTTP 200 ragChunks=3)
- איה (מייסדת): ✅ go מפורש
- אדם (CEO): לא נדרש — patch אבטחה פנימי, אפס user impact

## Jira
אין issue ייעודי — תיקון אבטחה פנימי שנבע מ-`npm audit`, אפס user impact. תועד כאן וב-release-reports.
