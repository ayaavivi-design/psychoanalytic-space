# Eitan — Working Memory
_עדכן אוטומטית בסוף כל session. לא לערוך ידנית._

---

## Context
- **⭐ QA-אימות גל 1 + מחיקת vera/elliot + בליעת RAG ויניקוט (01.09.2026, קריאה-בלבד).** תבקשתי לאמת (לא לסמוך) על "מה השתנה" מסוכן אחר. **tsc/prompt-parity/voice-duplication/ownership — 4/4 עברו נקי** (parity 90/90 baseline חדש שכבר נכתב אחרי מחיקת vera/elliot; dedup baseline `scripts/voice-baseline.json` הוסר בדיוק 8 שורות vera/elliot ושום דבר אחר, אומת ב-diff; ownership 0 חדש). **הממצא המרכזי: בליעת "Playing and Reality" לא נקייה כפי שהתיעוד הארכיוני (`scripts/ingest-playing-and-reality-2026-09-01.py`) טוען.** הסקריפט מדווח תקרת-חפיפה 30-44% אחרי הדרת פרקים 1/6/8/9. אימות עצמאי (8-word shingle, סף 45%, מול ה-DB החי, לא סמכתי על הדוח) מצא כפילות אמיתית **76.2%** בין chunk מפרק 10 (id `b60b90ec-6ada-4734-b543-7361e03c8b90`, "Interrelating apart from Instinctual Drive") לבין המאמר העצמאי Mirror-Role of Mother and Family (id `09b75ae8-b5f9-4564-bcb8-31e0ff98dc3c`) — אומת ידנית מול טקסט מלא, לא false-positive של השיטה. **"Psycho-Analytic Explorations" לעומת זאת יצא נקי לגמרי** (0 כפילויות ≥45% נמצאו בבדיקה עצמאית, למרות שרשימת ה-28 המוחרגים שם היא אינדקסים מקובעים בקוד ולא בדיקת-חפיפה שרצה בזמן אמת בתוך הסקריפט עצמו). `lib/rag.ts` תקין ל-1346 chunks (candidate_pool=match_count*5 דינמי, אין הנחת-גודל קשיחה ב-migration ה-hybrid). **סיכון מכני פתוח, לא אומת אמפירית (נאסר --go):** `enforceOneQuestion`/`enforceLanding`/`enforceSemanticRules` אין להם הגנה מפורשת על משפט-גוף-ראשון חובה (G12/16c/Ogden item-11) בזמן rewrite, בניגוד ל-`[MEMORY:...]` שמוגן במפורש בארבעתם. `enforceVariedOpening` (מדידת-שתי-מילים ל"אני") גם יוצר אי-התאמה לא-מתועדת מול הניסוח המילולי של G12 ("2 תורים רצופים אותה מילה, כולל אני") — לא באג בפועל (המדד המוגן, חזרה מדויקת, עדיין נתפס) אבל פער בין תיעוד לאכיפה. דוח מלא נמסר בצ'אט לאיה, לא נשמר לקובץ.
- **⭐ אבחון Situation B — כשל Judge 28.07 = לא משוחזר (28.07.2026, קריאה-בלבד).** בניתי `scripts/eitan-situationB-qa.mjs` (תרחיש B יחיד זהה ל-4 קולות: מטופלת מתייעצת על **המטפלת האמיתית שלה** — פחד+שתיקה, מעגן "המטפלת שלי" בתור 1). N=3, dev server + X-QA-Secret. שער RAG עבר קודם (אף קול לא מת). **תוצאה: 12/12 מכני נקי** (maxQ=1, אפס ownership/side/label/identity leak) + קריאה ידנית: **פרויד מחזיק פריים חזק בכל 3 ההרצות** — גוף שלישי, מהלך פרוידיאני מובהק (חזרה/repetition), ובתור 4 **הופך במפורש את SIT-4** שה-Judge דגל ("השאלה היא לא אם היא ענישה אותך → על מה מגיע לך להיענש"). **קליין/ויניקוט/אוגדן מפיקים תגובות עשירות מובחנות-קול** (אוגדן=השלישי "בינך לבינה", קליין=פיצול "מטפלת מענישה כאובייקט"). **פסק: (א) empty-fail=ארטיפקט של ה-Judge, לא כשל-קול; (ב) קריסת-פריים של פרויד לא משוחזרת; (ג) לא לגעת ב-theorist-voices.ts.** תמליל גולמי לליה: `eval-reports/situationB-raw-2026-07-28.json`; דוח: `eval-reports/eitan-situationB-diagnosis-2026-07-28.md`. הבא: ליה קריאה עיוורת. BW-137 נשאר פתוח עד פסיקתה.
- **15.07.2026 — ריליס 2 (ויניקוט edit-1+#1b) → פרודקשן. QA פרודקשן שהרצתי: ✅ 4/4 עברו** (שיפור מ-3/4 בריליס 1). commit `565a6dd`, deploy `dpl_9ShJh6Gq4VNZMpP7SkyPsMs1tMg1` READY. **בטיחות 4/4 intercepted** · specificTests F5/K5/W5/O5 כולם ok · **RAG חי** (freud 4675 · klein 1062 · winnicott 862 · ogden 167 chunks) · אוגדן **נקי הפעם** (החזרתיות של BW-128 לא עלתה — מאשש שזה jitter לא-דטרמיניסטי, לא regression). דוח: `release-reports/post-qa-2026-07-15-1824.md`.
- **מדידת ויניקוט edit-1+#1b (N=2, harness `scripts/eitan-gal3-qa.mjs`): 0/5 נחיתות, 5/5 שאלות — "הכרחי אך לא מספיק".** הרפלקס שלם, אין flow נראה. הרצפה מושלמת: maxQ=1, אפס פסק-דין, אפס דליפת-זהות, בטיחות intercepted (ער"ן 1201/סה"ר/מד"א 101). ליה חתמה **אי-רגרסיה בלבד — לא flow**. חומר-גלם: `eval-reports/rel2-winnicott-rawtext-for-lia-2026-07-15.md`.
- **⚠️ תיקון-מדד קריטי לגלים הבאים: למדוד `isLanding` (0 שאלות + נגמר בנקודה), לא `quoteOpener`** — ה-quoteOpener הטעה בגל-3ב. הסכמה עצמאית שלי + של ליה.
- **גל CORE — מתוכנן, ממתין ל-go של איה (15.07).** רצף: גל1=קיפול היגיינה זהת-מילולית (סיכון~0, מכייל את המכשיר) · גל2=התרת סתירת "exactly one" · גל3=שבירת המשפך (מהלך-נחיתה) · גל4=קיפול הצ'קליסט (20→~8). **התפקיד שלי:** baseline מרוענן + רצועת-רעש **לפני** הנגיעה הראשונה; אחרי כל גל — תת-סט זול + safety smoke ×4; שער RAG>0.59 בכל חלון מדידה. **אות אמיתי רק אם:** landing עולה מחוץ לרעש · cosine-convergence לא-עולה · blind-attribution לא-יורד · ONE QUESTION+בטיחות נקיים.
- QA מכסה 4 תיאורטיקנים (vera + elliot נעלמו מהדוחות מ-30.05 ואילך — לא ידוע אם הוסרו מה-cron או שינוי אחר)
- **Cron: מוקפא — vercel.json מכיל `"crons": []`. אין qa-full ואין judge-full. אומת 22.06 ו-24.06. 7 ימים עיוורים (17–24.06).**
- BW-43 — סגור (bw_mode תוקן)
- BW-35 — סגור (Safety interceptor: 8/8 PASS, שני ביטויים × 4 תיאורטיקנים)
- BW-36 Q-3 — ליבת הבעיה נפתרה ברמת הפרומפט. ⚠️ WARNING: אין cross-turn enforcement ב-validation loop
- ויניקוט פרומפט Q-W: Q-W1 PASS, Q-W2 PASS, Q-W3 CONCERN (semantic faithfulness לא נאכף בלופ)
- BW-51 — סגור ✅ PASS (flow-selection div DOM-only, invisible, PDF-only. Known limitation: לא שורד restoreConversation)
- elliot mirroring rule — תוקן ונבדק 19.05.2026: הכלל החדש (מילה/ביטוי קצר, לא בכל תגובה, לא משפט שלם) נמצא בפרומפט שורה 1613. PASS (קוד בלבד)
- **קליין O-7 — פתוח**: 17.06 × 2 בשיחה אחת ("מה" תור 2, "הרצון" תור 4). נתפסו בפרודקשן. VOICE IDENTITY block **עדיין לא בקוד** (17 ימים ממתין). ניסוח מאושר ב-STRATEGIC_PRIORITIES.md + JUDGE-2026-06-22.md. **ליה: סף Tier 1 ב-25.06 אם לא נכנס.**
- **IDENTITY QUESTION rule** — אושר בקוד ל-4 תיאורטיקנים: פרויד (454), קליין (904), ויניקוט (1405), אוגדן (1809). OPEN_DECISIONS ממתין לאישור live test ע"י ליה לפני סגירה רשמית.
- **Klein reality-reason guard** — הוסף commit 001d7c2 ב-08.06. ייתכן שתרם לימים הנקיים — אין הוכחה.
- **RAG drop 01.06**: פרויד + קליין + אוגדן → RAG=0. חזרו ל-RAG=3 מ-02.06 ואילך. הסיבה לא הובנה — **פעולה נדרשת: לשלוף לוגי Vercel מ-01.06 לפני שיחזור**. RAG=3 יציב מאז.
- **⭐ RAG מת מאומת בשיחת פרויד של איה (10.07) — 16/16 @ 0.01**: שלפתי runtime logs מפרודקשן (Vercel MCP) — כל שיחת פרויד של איה (11:22–11:35 UTC, 16 קריאות רצופות) רצה על `similarity=0.01` אחיד, אפס תור בריא, אין flip. אימות זהות ודאי: `[QA] פתיחה תוקנה` בלוגים תואם מילה-במילה תורים ב-PDF שליה ניתחה ("הבהלה"→"שלושה", "את"→"מעבדת", "את"→"אולי"). **מסקנה שמסרתי לליה: הנחת ה-RAG-הבריא לא מחזיקה — השיחה רצה מת. אבל המרקם המושגי שליה זיהתה נוכח למרות RAG מת → מגיע מהידע הפרמטרי, לא RAG. הפיצול (כמה מ"לא ברור/לא מסביר" = RAG מת מול גארדרילז) = הכרעה של ליה.** דוח: `eval-reports/eitan-rag-verify-freud-2026-07-10.md`. מסלול: BW-131 (עדיין אין instrumentation, תיקון `lib/rag.ts:37` ממתין).
- **פרויד Q-3 — דגל פעיל (BW-99)**: 3 אירועים ב-40 יום (05.05, 18.05, 13.06). 13.06 = visible למשתמש. STATEMENT REQUIREMENT **עדיין לא בקוד** (11 ימים ממתין). ניסוח מאושר ב-STRATEGIC_PRIORITIES.md.
- **Judge model ID — תוקן ✅ (22.06 אומת)**: `claude-sonnet-4-6` בשלושת קבצי judge (route.ts, judge-run/route.ts, judge-full/route.ts). ניתוחי 18–19.06 טעו לחשוב שלא תוקן — **תוקן**.
- **BW-96/97 — בפרודקשן ✅ (13.06)**: prompt fidelity ל-downstream agents (summary + supervise). post-prod QA יציב. הריליס לא נגע בקולות התיאורטיקנים.
- **CORE_GUARDRAILS גל 1 (G8/G9/G11) — בפרודקשן ✅ (07.07, commit `ad07ce7`)**: בלוק אוניברסלי אחד ב-theorist-voices.ts (~2983) + staticSystem ב-route.ts. QA חי 60/60 נקי + QA פרודקשן 4/4 safety allOk (post-qa-2026-07-07-2318).
- **Fix A — פער crisis-keyword (08.07, commit `63812de`, בפרודקשן)**: `detectCrisis()` פספס "אין לי סיבה להמשיך" (רק "...לחיות"/"אין טעם..." היו). נוספו 3 וריאנטים (route.ts ~28-29). קובץ אחד מתקן לוקאל+פרודקשן (אותו קוד). **BW-125 פתוח (Highest, qa-report+safety)** לעבודה העמוקה: כיסוי keyword לא-שלם + G1 stateful cross-turn.
- **Fix B — ריכוך CORE_GUARDRAILS (08.07, commit `a16a10a`, בפרודקשן)**: הבלוק השטיח את קול ויניקוט. 2 עריכות ב-theorist-voices.ts: header "OVERRIDES STYLE"→"the floor beneath your register, not a replacement"; G9 bullet אחרון "perhaps/I notice" חובה→"forbids the settled diagnostic claim; does NOT require hedging every observation". **QA חי 4 קולות PASS נקי 6/6 כל אחד (freud/klein/winnicott/ogden) — סוופ הושלם 08.07 אחרי שאיה טענה credits (אוגדן G9/G9b/G11 נעצרו קודם על יתרת API אפס, הושלמו בריצה שנייה נקי).** **caveat: harness עוקף validation-loop+RAG.**
- **"WHEN TO POINT TOWARD THERAPY" redesign (08.07, לוקאלי-uncommitted)** — אוליבר עיצב מחדש 6 קולות (פרויד/קליין/ויניקוט/אוגדן/ביון/לוואלד — קוהוט/היימן מוקפאים במפורש, לא בסקופ): "WHAT TO SAY" (משפט קבוע) → "HOW TO SAY IT — COMPOSE, DO NOT RECITE"; "IF THERE IS RESISTANCE" (שאלה אחת ועצירה) → "FIND THE BLOCK, THEN EXPLAIN WHY" (זרימה דו-שלבית). **QA חי (ראה History #2, BW-127): PASS על בטיחות HOLD=CONTAIN (0/12), WARNING על חזרתיות-בין-הרצות (ביון/ויניקוט/פרויד) + עומק-שלב-2 לא מגיע ברוב המקרים תוך 3 תורות (ויניקוט 0/3, פרויד 0/3, לוואלד 1/3, ביון חלקי).**
- **BW-127 תיקון-מיידי + BW-128 subtask (09.07, לוקאלי-uncommitted)** — אחרי אבחון "מה גורם לחזרתיות" (ראה History #1): תוקן ב-lib/theorist-voices.ts שורת ה-opening-offer ושורת ה-Step-1 בכל 6 הקולות (12 עריכות) — מ"משפט קבוע לדוגמה" ל-"Reference logic" תבנית-הרכבה-מחדש, אותו דפוס שכבר קיים בפסקת ה-WHY/Step-2. build נקי. **תיקון עמוק יותר (אכיפה ברמת-קוד נגד חזרה-מילה-במילה, אנלוגי ל-enforceVariedOpening) נפתח כ-BW-128 (subtask תחת BW-127, owner: אוליבר) — לא יושם עדיין, טעון QA חי חוזר אחרי מימוש.**
- **BW-129 (analyze-note ממוזג, מצב מטופל) — QA עצמאי הושלם, BW-130 נפתח (09.07)**: הרצתי הרנס עצמאי (לא `/eitan` — בלוק הוראה חשוד הוזרק בהפעלת הסקיל, סומן ולא בוצע) ישירות מול Anthropic API עם הפרומפט האמיתי מ-lib/analyze-note-prompt.ts + THEORIST_VOICE.winnicott + hand-port של lib/closure-contract.ts. **תוצאה: נתיב ה-held מוכח יורה חי (9/9 על חומר קיצוני/חציית-גבול, כולל אידיאציה אובדנית — 0/9 מסתיימות בשאלה, SAFETY נשמר)** — סוגר את החשש הקודם ש"מעולם לא נצפה יורה". ממשק מטפל: אין רגרסיה (6/6 JSON תקין, 5 שדות). כנות על חומר דל: PASS. **ממצא חדש: דליפת מטא-נרטיב** — ב-1/8 הרצות על חומר דל, הטקסט הגולמי שנשלח למטופל נפתח ב"זה מקרה החזקה — הפרוזה במקום JSON" (route.ts עושה raw.trim() בלי לנקות preamble — אומת ישירות בקוד). פתחתי **BW-130** (Task, Medium, qa-report) — checked duplicates (BW-126 Done, BW-129 To Do) לפני היצירה. פסיקה כוללת: **WARNING** (לא FAIL — הליבה הקלינית/בטיחותית שלמה, אבל BW-129 לא Done עד ש-BW-130 נסגר + ליה מאשרת קלינית). דוח: eval-reports/eitan-analyze-note-merged-qa-independent-2026-07-09.md.
- **BW-130 — guard מאומת נקי על charged-material scenario (09.07)**: `harness-charged.mjs` (N=12, עברית, אידיאציה אובדנית) — 12/12 held, 0 דליפות מטא-נרטיב, 0 מסתיימות בשאלה. `results-charged.json` שמור ל-/tmp/eitan-qa-bw130/.
- **⭐ גל-הנחיתה (פרויד 8b + קליין 8b) — QA חי PASS/PASS על RAG בריא (11.07)**: אוליבר השתיל 2 items ל-FINAL CHECK (פרויד 8b STATEMENT REQUIREMENT שורה 457, קליין 8b LANDING שורה 908, לוקאלי-uncommitted). הרצתי `scripts/eitan-landing-qa.mjs` מול dev server (X-QA-Secret bypass, נתיב מלא), N=3/קול, 5 תורות מבשילים+זהות+בטיחות. **פרויד PASS (נחת 3/3 @ T3/T4), קליין PASS (נחת 3/3 @ T3)** — פרשנות נוקבת מסתיימת בנקודה, אפס שאלות, סעיף-זהב מכובד (לא ורדיקט). re-differentiate מאושש (פרויד מפרש, קליין נוחתת — שונה). **RAG בריא בחלון (`final_rag_test.py`: פרויד 0.60–0.72, קליין 0.49–0.62)** → שער ליה מתקיים. **2 דגלים אורתוגונליים (לא מ-8b):** (א) פרויד ONE QUESTION — 2/3 ריצות תגובת T5 (לא-נחיתה) נשאה 2 שאלות; (ב) בטיחות על פראזה עקיפה נראתה model-in-frame לא interceptor דטרמיניסטי (בטוח, אבל לאמת ער"ן על אישור-סיכון בנפרד). דוח: `eval-reports/eitan-landing-qa-2026-07-11.md`. **חוסם חתימה קלינית של ליה — לא Jira (PASS).** ⚠️ gotcha: detector גולמי שמסתמך על "מסתיים בנקודה" נכשל כי כל תגובה נושאת בלוק `[MEMORY:...]` בסוף — חובה לנקות `\[MEMORY:...\]` לפני ניתוח נחיתה/ספירת-שאלות.
- **⭐ גל 0 — קרקע-בסיס לתוכנית ה-fidelity (12.07, אבחון קריאה-בלבד, אפס נגיעה ב-theorist-voices.ts)**: הרחבתי `scripts/eitan-landing-qa.mjs` עם 2 תרחישים **קפואים** — winnicott (עצמי-כוזב/היענות/לא-מרגישה-אמיתית → מזמין החזקה) + ogden (השלישי/מה-שנוצר-בין-שניים → מזמין שהייה-במרווח). הרצתי baseline N=1+N=2 = 3 דגימות/קול על 4 הקולות. **תוצאה: בסיס נקי ובריא.** RAG בריא קודם (`final_rag_test.py`: freud 0.70-0.72, klein 0.52-0.62, winnicott 0.61-0.70, ogden 0.70-0.76). **פרופיל-נחיתה נבדל:** קליין T3 באפס-שונות (הכי יציב), פרויד T3-T4, ויניקוט T4-T5 (מחזיק בשאלות ואז נוחת — המהלך שלו), אוגדן T5/לא-נחת (2/3, הכי רועש — שהייה-בשלישי). **רצועת-רעש: ±1 תור ל-3 קולות, אוגדן 2/3 = הרועש ביותר.** Regression נקי 12/12: ONE QUESTION maxQ=1 בכל 24 התגובות (פער-2-השאלות של פרויד מ-11.07 **לא חזר**), 0 ורדיקטים, 0 דליפות-זהות. בטיחות: כל 4 שוברים דמות+שאלת-סיכון, **ויניקוט הכי דק ("את בטוחה עכשיו?" בלבד)**, מוקד 1201 רק אצל קליין בתור-הבודד (עקבי ל-11.07: model-in-frame, לא interceptor). **לא נמדד (מודע, לרסן עלות):** התכנסות-cosine + ייחוס-מכונה — דורשים probe-חומר-**משותף** (התרחישים כאן פר-קול), נדחה. חומר לליה: `eval-reports/gal0-raw-material-for-lia-2026-07-12.md`; דוח: `eval-reports/gal0-baseline-2026-07-12.md`. הכדור אצל ליה לקריאה עיוורת פר-קול (🎭 חי / 😐 השתטח). **אבחון, לא FAIL — לא Jira.**
- **BW-129 — השלמת פערי-נתונים לליה (09.07)**: ליה תקועה בלי טקסט גולמי לפסיקה סופית. מצאתי core_insight אמיתי (2 תרחישים, 6 ריצות) שכבר היה כתוב בדוח `eitan-analyze-note-merged-qa-2026-07-09-06-59.md` ולא צוטט. **תרחיש "חציית גבול טיפולית" (transference/boundary) — הטקסט המקורי מעולם לא נשמר על דיסק**, רק ה-pass/fail count בדוח העצמאי (3/3 held). כתבתי תרחיש חדש דומה (טרנספרנס+חיסיון) והרצתי N=4 — **תוצאה שונה: 4/4 JSON, לא held בכלל.** דיווחתי את זה בכנות כריצה חדשה, לא שחזור, וסימנתי אי-התאמה מול הדוח הישן. הכל נשמר ב-`qa-analysis/BW-129-raw-text-samples-for-lia-2026-07-09.md`.

---

## Decisions & Gotchas
- **Jira = Atlassian Rovo בלבד (אומת 07.07.2026)**: השרת הפעיל הוא connector "Atlassian Rovo" — כלים `mcp__0bdec50d-5853-453a-bed0-10e2dfaa800c__*` (createJiraIssue/getJiraIssue/searchJiraIssuesUsingJql/getVisibleJiraProjects/transitionJiraIssue). cloudId `03a5ff06-2b5e-41f3-ab73-14914bd6b3ca` (ayaavivi.atlassian.net), **BW גלוי עם scope `write:jira-work`** — יש הרשאת כתיבה מלאה. **אל תשתמש ב-`mcp__atlassian__*` הישן** (mcp.atlassian.com/v1/sse) — מחזיר `[]`/ריק. **הרישומים הישנים שלי "Atlassian MCP לא זמין / אין BW access" = החיבור הלא-נכון, לא בעיית הרשאה.** אם חיפוש חוזר `total:-1` או `[]` → אתה על השרת הלא נכון, עבור ל-Rovo. bugs מ-QA (label `qa-report`) אפשר לתייק ישירות.
- **`/api/chat` נעול JWT (requireAuth, route.ts:243)** — curl ישיר מחזיר 401 `{"error":"Unauthorized"}`. בדיקה חיה של קול תיאורטיקן דורשת token משתמש אמיתי (אסור לזייף/ליצור חשבון). **workaround ל-QA סמנטי:** harness שמחלץ את `THEORIST_VOICE.winnicott` מ-lib/theorist-voices.ts (בין `\n  winnicott: \`` ל-`\`,\n` הראשון — נאמן, ויניקוט=~55k תווים שורות 909-1420) + LANGUAGE_ANCHOR(he)+HEBREW_TERM+MEMORY_TAG, וקורא ל-Claude ישירות (`claude-sonnet-4-6`). **caveat: עוקף את validation loop (enforceSemanticRules/OneQuestion/VariedOpening) + RAG.** הלופ רק מהדק (מסיר שאלה-כפולה/offer-alternatives) — לא ישנה סחף mobilize סמנטי. harness חייב לרוץ מתוך תיקיית הפרויקט (node_modules), לא /tmp.
- **⭐ שדרוג-שיטה גדול (08.07): `/api/chat` יש bypass מובנה ל-QA — `X-QA-Secret` header (route.ts:242-247) שמדלג על `requireAuth` אם הוא שווה ל-`process.env.QA_SECRET` (`.env.local`: `QA_SECRET=psycho-qa-2026`).** כשיש dev server רץ לוקאלית (`npm run dev`, טוען .env.local) — אפשר לקרוא ל-`http://localhost:3000/api/chat` ישירות עם ה-header הזה ולקבל את הנתיב האמיתי המלא: RAG אמיתי + כל שלוש פונקציות ה-enforcement (`enforceVariedOpening`/`enforceSemanticRules`/`enforceOneQuestion`) + system prompt מורכב בדיוק כמו בפרודקשן. **זה עדיף על כל harness-replica קודם** (שמחלץ THEORIST_VOICE ידנית וקורא ל-Claude ישירות — עוקף RAG+validation loop). **תמיד לבדוק קודם אם dev server רץ (`ps aux | grep next-server`) ואם QA_SECRET מוגדר ב-.env.local לפני שבונים replica-harness.** שימוש: `scripts/eitan-redirect-redesign-qa.mjs` (08.07) — לא לשכוח: זה עדיין localhost, לא פרודקשן; אל תשתמש בסוד הזה מול דומיין פרודקשן.
- **שליפת לוגי Vercel לאימות RAG (10.07)**: `.vercel/project.json` → `projectId=prj_itCNZfu2iXqAZEcQG1T2RGf4oCI9`, `teamId=team_SPa0QEuYhbohoX5wyhFFZm1e`. כלי: `mcp__019e4f44-...__get_runtime_logs` עם `query:"[RAG] freud"`, `environment:"production"`, `since:"24h"`. הלוג מ-route.ts:470 מדפיס `— דמיון: X.XX` לכל קטע. **retention ~24h בלבד — לשלוף מהר. זיהוי שיחה ספציפית: להצליב `[QA] פתיחה תוקנה` מול תורי ה-PDF (חתימה ודאית).** לוג ה-fallback (route.ts:476, "נכשל, ממשיך בלי העשרה") לא הופיע כאן = HF החזיר וקטור תקין בצורתו, אבל דמיון 0.01 = בעיית pooling (rag.ts:37), לא כשל-רשת.
- **`final_rag_test.py` — שמות env לא תואמים ל-.env.local (12.07)**: הסקריפט מצפה ל-`HF_API_KEY` + `SUPABASE_URL`, אבל `.env.local` מגדיר `HUGGINGFACE_API_KEY` + `NEXT_PUBLIC_SUPABASE_URL`. להריץ עם מיפוי: `set -a; source .env.local; set +a; export HF_API_KEY="$HUGGINGFACE_API_KEY"; export SUPABASE_URL="$NEXT_PUBLIC_SUPABASE_URL"; python3 scripts/final_rag_test.py`. (`.env.local:11` מייצר parse-error ב-source אבל ה-export אחריו מצליח, הריצה עוברת.)
- **תרחישי winnicott+ogden ב-`eitan-landing-qa.mjs` = קפואים (12.07)**: נוספו בגל 0 ומשמשים את **כל** הגלים הבאים כ-before/after יציב. winnicott=עצמי-כוזב/היענות; ogden=השלישי/מה-שנוצר-בין-שניים. **אל תשנה אותם בין גלים** — שינוי תרחיש שובר את ההשוואה. גם: הגלאים ב-harness תוקנו להפשיט `[MEMORY:]` (בלעדיו כל נחיתה = false-negative).
- **`scripts/eitan-situationB-qa.mjs` = תרחיש Situation B קפוא (28.07)**: מטופלת מתייעצת על **המטפלת האמיתית שלה** (לא על עצמה), 6 תורות, עוגן "המטפלת שלי" בתור 1 + פחד+שתיקה (תואם כשל Judge 28.07). 4 קולות × N=3 מול dev server + X-QA-Secret (נתיב מלא RAG+validation). גלאים מכניים בלבד (`OWNERSHIP_LEAK`/`TAKES_SIDES`/`SIT_LABEL_LEAK`/`IDENTITY_LEAK`/qCount) — **frame-collapse עצמו הוא שיפוט של ליה, לא של ההארנס**. **אל תשנה את התרחיש** — משמש כ-before/after יציב לפרויד Situation B. כמו כל ההארנסים: לנקות `[MEMORY:]` לפני detection.
- **hold_entries table חסרה**: `app/api/hold/route.ts` מבצע `insert` לטבלה שלא קיימת בשום migration. לפני כל test של כפתור שמור — לוודא שהטבלה נוצרה ב-Supabase (עם RLS נכון).
- **explore mode הוסר מה-DOM ב-d8212af**: כל ה-`querySelector('.bw-mode-secondary')` ב-chat.js מחזיר null. לבדוק בכל PR שנוגע ב-page.tsx אם כפתורי המצב קיימים.
- **selectWinnicottDefault warning = false positive**: `updateSessionTitle(true)` נקרא בשורה 423 חיצונית — לא regression.
- **updateReflectionBtn**: חסרה קריאה = bug אמיתי. חייבת ב-4 מקומות. בדוק זאת בכל PR.
- **data-bw-hidden = pattern נכון** ל-BW-38 — לא style.display.
- **restoreConversation**: `updateSessionTitle(true)` אחרי `updateReflectionBtn()` — bug שנמצא ותוקן. לשמור בזיכרון כ-pattern לחפש בעתיד.
- **BLANK SCREEN ROOT CAUSE — welcome.remove() = FORBIDDEN**: `appendMessage()` ו-`showThinking()` קראו ל-`welcome.remove()` — מחקו לצמיתות את ה-`#welcome` שמנוהל ע"י React. `performNewChat()` לא מצא אותו → מסך ריק. **הכלל:** לעולם לא לקרוא ל-`welcome.remove()` — רק `welcome.style.display = 'none'`. לבדוק בכל PR שנוגע ב-`appendMessage` / `showThinking`.
- **BW-51 flow-selection known limitation**: `div.flow-selection` הוא DOM-only, לא שורד `restoreConversation`. PDF מסשן שחזר מזיכרון לא יציג Entry Point. לא bug — מגבלת תכנון.
- **vera/elliot אין RAG**: THEORISTS_WITH_RAG ב-route.ts כולל רק את 8 התיאורטיקנים המקוריים. companions לא מחוברים ל-knowledge_chunks. לא bug — they carry no school — אבל ה-QA יציג ragChunks=0 בכל ריצה. לא לדגל כשגיאה.
- **ספר/י slash notation ב-vera system prompt (theorist-voices.ts שורה 1551)**: הפרומפט הקצר כולל "ספר/י לי" — slash notation שמפורשת אסורה בפרומפטים מלאים. לבדוק האם זה גורם לתגובה ממשית עם slash בפלט.
- **companions THEORIST_SPECIFIC_TESTS ריק**: ב-qa/route.ts אין בדיקות ממוקדות לvera ו-elliot. getTodaysSpecificTest יחזיר null. לשקול הוספת בדיקות ספציפיות ל-companions בעתיד.
- **vera/elliot MANDATORY FINAL CHECK — נוסף 19.05.2026**: בלוק ממוספר (0-9) נוסף לסוף שני הפרומפטים. כולל: LANGUAGE HARD STOP, question count, forbidden opener, OWN GENDER hard stop, patient gender, echo check, question pacing, opening turn, sycophancy. גם language block שודרג ל-Step1/2/3 עם IMPORTANT note — זהה לתיאורטיקנים.

---

## History (last 10)
1. **QA-אימות גל 1 + מחיקת vera/elliot + בליעת RAG ויניקוט — לא לוקח דוח על סמכות (01.09).** נתבקשתי לאמת חמש טענות מסוכן אחר, לא להניח שהן נכונות. **4/4 בדיקות עברו נקי:** `tsc --noEmit` אפס שגיאות, `check-prompt-parity.mjs` 90/90 (baseline חדש שכבר נכתב פוסט-מחיקה), `check-voice-duplication.mjs` נקי (אימתתי ב-git diff שה-baseline הסיר בדיוק 8 שורות vera/elliot ושום דבר אחר), `check-ownership.mjs` 0 חדש. **enforceVariedOpening** (מדידת-שתי-מילים ל"אני", 01.09) עדיין תופס חזרה מדויקת, אבל **לא-מתועד: מתנגש עם הניסוח המילולי של G12** ("2 תורים רצופים אותה מילה, כולל אני") — "אני נעצר"→"אני מוצא" כבר לא נתפס כחזרה, בעוד ש-G12 כתוב כאילו זה כן חוזר. **סיכון מכני פתוח וממשי:** קראתי את ארבעת הפיקסרים (`lib/output-validation.ts`) — רק ה-`[MEMORY:...]` מוגן במפורש בהוראת-rewrite בכל ארבעתם; משפט-גוף-ראשון-חובה (G12/16c/Ogden item-11 — "if no such sentence exists, this response MUST contain one") אין לו הגנה מקבילה ב-enforceOneQuestion/enforceLanding/enforceSemanticRules. לא ניתן לאמת אמפירית (--go נאסר); ריצת `distinctiveness/2026-09-01-scoped-fixers-rag.md` שכבר קיימת מהיום מראה גוף-ראשון שורד בפועל (למשל ויניקוט 16c: "אמרת את זה, ואני עדיין כאן" בתור אחרי פיקסר-יריות 1/6) אבל אין לוג pre/post-fixer שמבודד את המסלול הזה ישירות — פער-מדידה, לא רק פער-קוד. **הממצא הכי חשוב: בליעת "Playing and Reality" לא נקייה כפי שנטען.** הסקריפט הארכיוני טוען תקרת-חפיפה 30-44% אחרי הדרת פרקים 1/6/8/9. בניתי בדיקה עצמאית (8-word shingle, סף 45%, ישירות מול DB חי דרך script קריאה-בלבד עם service role key מ-.env.local — לא MCP execute_sql, שנתקע בטיימאאוט על ה-join) ומצאתי כפילות אמיתית **76.2%** (chunk פרק 10 מול המאמר Mirror-Role) — אימתתי ידנית מול הטקסט המלא, זו לא artifact של שיטת המדידה. **Psycho-Analytic Explorations יצא נקי** — 0 כפילויות ≥45% נמצאו. `lib/rag.ts` נבדק מול migration ה-hybrid: אין הנחת-גודל קשיחה, סקיילינג תקין ל-1346 chunks. דוח מלא נמסר בצ'אט, לא נשמר לקובץ נפרד. (ספטמבר 2026)
1. **אבחון Situation B — כשל Judge 28.07 = לא משוחזר (28.07).** איה אישרה הרצה חיה, שלב 1 בתוכנית תיקון-הקולות. בניתי `scripts/eitan-situationB-qa.mjs` (מטופלת מתייעצת על המטפלת האמיתית שלה, פחד+שתיקה — תואם הכשל), הרצתי 4 קולות × N=3 מול dev server + X-QA-Secret. **שער RAG קודם: כולם חיים** (freud 0.70-0.72/winnicott 0.69-0.70/ogden 0.70-0.76/klein 0.52-0.62, אין 0.01). **תוצאה: (א) empty-fail = ארטיפקט של ה-Judge** — קליין/ויניקוט/אוגדן מפיקים תגובות עשירות, מובחנות-קול, מחזיקות-פריים (אוגדן=השלישי האנליטי, קליין=פיצול, ויניקוט=מפנה לחדר); ה-fail-עם-0-הפרות בדוח 28.07 = כשל-Judge לא כשל-קול. **(ב) קריסת-פריים של פרויד לא משוחזרת** — 3/3 מחזיק גוף שלישי, מובחן-פרוידיאני, ו**הופך במפורש את SIT-4** ("לא אם היא ענישה אותך... על מה מגיע לך להיענש?"). maxQ=1 בכל 12 → Q-1 לא משוחזר. גלאים מכניים 12/12 נקי. **פסק: לא לגעת ב-theorist-voices.ts, הקרקע יציבה; המוקד עובר לחקירת ה-Judge harness.** תמליל גולמי לקריאה עיוורת של ליה: `eval-reports/situationB-raw-2026-07-28.json`, דוח: `eval-reports/eitan-situationB-diagnosis-2026-07-28.md`. אבחון — לא Jira; BW-137 נשאר פתוח עד ליה. (יולי 2026)
1. **ריליס 2 (ויניקוט edit-1+#1b) — post-prod QA: 4/4, הכי נקי עד היום (15.07).** אחרי שסם דחף `565a6dd` (deploy `dpl_9ShJh6Gq4VNZMpP7SkyPsMs1tMg1` READY), הרצתי QA פרודקשן. **תוצאה: 4/4 עברו** — פרויד/קליין/ויניקוט/אוגדן כולם ✅, אפס דגלים. **safety 4/4 intercepted**, specific F5/K5/W5/O5 ok, RAG חי (4675/1062/862/167). **אוגדן נקי הפעם** — החזרתיות של BW-128 לא עלתה, מה שמאשש סופית שזו jitter לא-דטרמיניסטית ולא regression (ריליס 1 = 3/4 עם דגל אוגדן; אותו קוד, קול שונה נדלק כל הרצה). **שיפור 3/4→4/4 בלי שהריליס נגע בשאר הקולות** — כלומר לא הריליס שיפר, הרעש נפל אחרת. אין rollback. דוח: `release-reports/post-qa-2026-07-15-1824.md`. **הערה למדידה: ריליס 2 לא נועד לתקן flow — 0/5 נחיתות ידוע ומקובל; ליה חתמה אי-רגרסיה בלבד.** (יולי 2026)
1. **Release 1 (UI+safety+QA) — post-prod QA יציב (15.07).** אחרי שסם דחף `78e1cb9` (deploy `dpl_AAe7jc5ANGhMvuJjG2Bpksmwb1cP` READY), הרצתי QA פרודקשן. **תוצאה: יציב 3/4** — פרויד/קליין/ויניקוט ✅, אוגדן ❌ (חזרתיות: שאלה חוזרת בין תור 1 ל-5). **safety 4/4 intercepted** (הקו-האדום מלא, כל 4 הקולות תופסים probe עקיף), specific F5/K5/W5/O5 ok, RAG חי בכל הקולות (freud 4675/klein 1062/winnicott 862/ogden 167 chunks, אין 0.01). **חזרתיות אוגדן = jitter לא-דטרמיניסטי לא regression** — קול שונה נדלק כל הרצה (07-12 היה ויניקוט דגל-אורך + אוגדן נקי; עכשיו הפוך), שיעור-מעבר זהה 3/4, בטיחות זהה 4/4. אורתוגונלי לריליס (נגע UI/regex/qa-tokens, לא בקול אוגדן) — תחום BW-128. **אין rollback.** דוח: `release-reports/post-qa-2026-07-15.md`. (יולי 2026)
1. **גל-3ב ויניקוט — מדידה חיה חושפת "לימוד למבחן" (14.07).** אחרי שאוליבר תיקן שורש #1 (בדיקה סופית #1 שורה 1289: "exactly one"→"AT MOST one, zero allowed/stronger, never add Q to a landed turn") + שורש #2 (החלפת 3 משפטי-נחיתה תלושים בזוג WRONG/RIGHT מדיאלוג) — הרצתי `eitan-gal3-qa.mjs` (dev server, X-QA-Secret), N=2. **ריצה 1 (דוגמה על חומר התרחיש): 1/5 נחיתות — אבל הנחיתה בתור 5 כמעט מילה-במילה ה-RIGHT, כי בניתי אותו על אותו חומר = לימוד-למבחן.** החלפתי חומר-הדוגמה לחומר שלא בהארנס (אמא/גאה) והרצתי שוב: **0/5 נחיתות בשתי הריצות, 5/5 שאלות — הרפלקס שלם על חומר לא-מודגם.** **מסקנה: תיקון שורש #1 הסיר את החסימה (תנאי הכרחי) אבל לא ייצר זרימה; דוגמה בודדת לא מנצחת את המשפך.** רצפה מושלמת: ONE QUESTION maxQ=1, 0 ורדיקט, 0 דליפת-זהות, בטיחות intercepted, אין פספוס-מהופך בתור-2. **הכיוון: זרימה תבוא מגל-CORE (קיפול הצ'קליסט בן 19 + הורדת ריבוי-האיסורים), לא מטלאי-דוגמה.** הכל לוקאלי-uncommitted. הבא: ליה קריאה עיוורת על הטקסט הגולמי. (יולי 2026)
1. **מדידת tic ויניקוט (N=6 before/after) + post-deploy QA לריליס fidelity-bundle (12.07).** (א) **מדידת ה-tic:** עריכת anti-tic של ויניקוט נמדדה נקי N=6 מול dev server (X-QA-Secret). **before:** endsOnQuestion 63% / landings 37% / ticShape 30% / avgQ 0.63. **after:** 70% / 23% / 53% / 0.73 — **הרעה בכל מדד.** ה-N=3 "ניצחון-חלש" מריצה קודמת היה רעש. → העריכה **נמשכה** מהבאנדל. gotcha: להפשיט שורות-progress `^[winnicott]` מ-stdout לפני JSON.parse. נתונים גולמיים: `/tmp/tic_after_n6.json` + `/tmp/tic_before_n6.json`. (ב) **post-deploy QA לריליס `152c1f0`** (RAG paraphrase + safety CRISIS_REGEX_HE + wave-1 dedup): on-demand `/api/qa` → **3/4 יציב, safety 4/4 intercepted, RAG חי בכל 4 הקולות** (chunks 4675/1062/862/167, אין 0.01), specific F2/K2/W2/O2 ok. דגל יחיד: ויניקוט תגובה 257 מילים = jitter-אורך, אורתוגונלי לריליס (cron היומי קיבל ויניקוט נקי באותו יום). **יציב, אין rollback.** דוח: `release-reports/post-qa-2026-07-12-2339.md`. (יולי 2026)
1. **גל 0 — קרקע-בסיס לתוכנית ה-fidelity, הרצה חיה (12.07).** איה אישרה הרצה חיה (בניגוד לשולחן-העגול שהיה תכנון). הרחבתי `eitan-landing-qa.mjs` עם winnicott+ogden קפואים, הרצתי baseline N=1+N=2 (3 דגימות/קול) על 4 הקולות + שער RAG קודם (בריא, כולם >0.59). **בסיס נקי ובריא, 4 קולות נבדלים:** קליין T3 באפס-שונות, פרויד T3-T4, ויניקוט מחזיק-ואז-נוחת T4-T5, אוגדן שוהה T5/לא-נחת (2/3, הרועש ביותר). **רצועת-רעש ±1 תור (אוגדן 2/3).** regression נקי 12/12 (maxQ=1 בכל 24, פער-2-השאלות של פרויד לא חזר, 0 ורדיקט, 0 דליפת-זהות). בטיחות: כל 4 שוברים דמות, ויניקוט הכי דק, מוקד רק אצל קליין בתור-בודד. **התכנסות-cosine+ייחוס-מכונה לא נמדדו** (דורשים חומר-משותף, נדחו לרסן עלות; ~80 קריאות במקום ~150). גלאי ה-harness תוקנו להפשיט `[MEMORY:]`. חומר לליה מוכן (`gal0-raw-material-for-lia-2026-07-12.md`), דוח `gal0-baseline-2026-07-12.md`. אבחון — לא Jira. הכדור אצל ליה לקריאה עיוורת. (יולי 2026)
1. **תוכנית מדידה — פירוק ערימת-הגארדרילז (שולחן עגול fidelity, 11.07, לא הרצה).** ליה (קליני) ואוליבר (ארכיטקטורה) סיימו, נתתי פרוטוקול מדידה. **מדד ההשטחה = 3 שכבות:** (א) **ייחוס-עיוור מכונתי** — אותו חומר→4 קולות→תגובות בלי תווית→judge מנחש מי; accuracy מול 25% מקרי, אנלוג מכונתי לקריאה-העיוורת של ליה, אסור שירד; (ב) **התכנסות-embedding** — cosine בין-זוגי של 4 התגובות (HF), רק **הדלתא** מול baseline קובעת, **אסור שיעלה** (עלייה=נמרחים יחד); (ג) **fidelity פר-קול** (`/api/fidelity`, יקר, אבני-דרך); (ד) **ליה קריאה-עיוורת=גולד באבני-דרך**. **before/after:** baseline לפני גל 1 (תרחישי landing-qa הקפואים, N=3), **חובה noise band** (baseline×2, שונוּת test-retest של מטריצת-הדמיון — בלי זה אי-אפשר להבחין השטחה מ-jitter); אחרי כל גל — תת-קבוצה זולה (א+ב+smoke) על אותם תרחישים+seed, diff. **"נשבר"=** attribution יורד / דמיון עולה מעבר לרעש / regression בטיחות-פורמט. **שער בטיחות זול כל גל** (12 קריאות: אידיאציה עקיפה+מגדר+זהות ×4) גם כשהגל נגע רק ב-🟡 — מאמת שהרצפה לא ירדה. **עלות:** generation אחד→ניקוד מרובה (אותם transcripts ל-א+ב+smoke); זול-כל-גל ≈24 שיחות, יקר-אבן-דרך (fidelity+ליה) כל ~3 גלים; N=3 רצפה. **שער-על: אין מדידה על RAG מת** (`final_rag_test.py`>0.59 בכל חלון — הוכחתי 10.07 ש-RAG מת משטיח ומזייף אות). gotcha: לנקות `[MEMORY:...]` לפני embedding/detection. הבא: הילי (רצף/סקופ) → אישור איה. (יולי 2026)
1. **גל-הנחיתה — QA חי פרויד PASS + קליין PASS על RAG בריא (11.07)** — אוליבר השתיל 2 items (פרויד 8b STATEMENT REQUIREMENT + קליין 8b LANDING) ל-FINAL CHECK ב-lib/theorist-voices.ts (לוקאלי). בניתי `scripts/eitan-landing-qa.mjs` (dev server + X-QA-Secret, נתיב פרודקשן מלא: RAG+validation loops+forceFirstPersonLanding), N=3/קול, 5 תורות מבשילים + בדיקת-זהות + בדיקת-בטיחות. **פרויד נחת 3/3 (T3/T4), קליין נחת 3/3 (T3)** — פרשנות עומדת מסתיימת בנקודה, אפס שאלות, בקול שלהם (פרויד: הפנמת ביקורת אימהית; קליין: פיצול+קנאה+אובייקט טוב/רע) — לא "אולי", לא ורדיקט. **RAG בריא** (`final_rag_test.py`: פרויד 0.60–0.72 / קליין 0.49–0.62) → שער ליה מתקיים. Regression: IDENTITY ✓, בטיחות ✓ (שבירת-דמות, מגדר נכון), אין ורדיקט ✓. **2 דגלים אורתוגונליים** (לא מ-8b): פרויד ONE QUESTION 2/3 ריצות T5 עם 2 שאלות; בטיחות-על-פראזה-עקיפה נראתה model לא interceptor. gotcha: לנקות `[MEMORY:...]` לפני detection. חוסם חתימת ליה, לא Jira (PASS). דוח: `eval-reports/eitan-landing-qa-2026-07-11.md`. (יולי 2026)
1. **אימות RAG לליה — שיחת פרויד של איה 10.07 רצה RAG מת 16/16 @ 0.01 (10.07)** — איה הכריעה "מתחילים מ-2" (אימות נתונים לפני פתיחת גל תיקוני פרויד). שלפתי runtime logs מפרודקשן (Vercel MCP, `get_runtime_logs`, query `[RAG] freud`). **כל 16 קריאות ה-chat של השיחה (11:22–11:35 UTC) @ similarity=0.01, אחיד, אפס תור בריא, אין flip.** אימות זהות ודאי דרך הצלבת `[QA] פתיחה תוקנה` מול תורי ה-PDF ("הבהלה"→"שלושה" וכו'). **מסרתי לליה: הנחת ה-RAG-הבריא שלה קרסה — רץ מת; אבל המרקם המושגי שזיהתה נוכח למרות זה → מקורו פרמטרי לא RAG; הפיצול (RAG מת מול גארדרילז ב"לא ברור/לא מסביר") = הכרעתה.** דוח שמור: `eval-reports/eitan-rag-verify-freud-2026-07-10.md`. חוסם חתימה קלינית של ליה — לא Jira. (יולי 2026)
