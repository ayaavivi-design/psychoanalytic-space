# Sam — Working Memory
_עדכן אוטומטית בסוף כל session. לא לערוך ידנית._

---

## Context
- **15.07.2026 — Release 2 (ויניקוט edit-1+#1b, שער קליני) → פרודקשן, READY:** commit `565a6dd`, deploy `dpl_9ShJh6Gq4VNZMpP7SkyPsMs1tMg1` READY production, sha 565a6dd. **Winnicott-only** ב-`lib/theorist-voices.ts` (41 insertions / 56 deletions, כל ה-hunks בין שורות 971-1313, בתוך סקשן ויניקוט 909-1423). תוכן: שורש #1 (FINAL CHECK #1 "exactly one"→"AT MOST one, zero allowed/stronger, never add Q to a landed turn") + שורש #1b (symmetric-landing checklist) + שורש #2 (משפטי-נחיתה מופשטים → זוג WRONG/RIGHT מדיאלוג מודגם) + dedup + stale-word. **שער: ליה חתמה אי-רגרסיה** (לא flow) — הקול לא נשבר, בטיחות מחזיקה, אין דליפת-זהות/פסק-דין. **מפורשות לא flow-pass: 0/5 נחיתות ידוע ומקובל** (edit-1+#1b = הכרחי-לא-מספיק; flow אמיתי = גל CORE עתידי נפרד). מדדים: maxQ=1, אפס ורדיקט/דליפה, safety intercepted (ער"ן 1201/סה"ר/מד"א 101). build EXIT=0, origin 0/0 בסנכרון (push נקי בלי rebase, 78e1cb9..565a6dd). איה go מפורש ("יש אישור"). אין Jira ייעודי (שינוי-קול prompt-level, לא BW-item). **מועמד rollback: `565a6dd`** (קודמו/מועמד-חזרה `78e1cb9` dpl_AAe7jc5). חומר-גלם: eval-reports/rel2-winnicott-rawtext-for-lia-2026-07-15.md.
- **15.07.2026 — Release 1 (UI+safety+QA, בלי שער קליני) → פרודקשן, יציב:** commit `78e1cb9`, deploy `dpl_AAe7jc5ANGhMvuJjG2Bpksmwb1cP` READY production. תוכן: BW-135 (טיוטת-composer ב-localStorage) + BW-134 (שם-קובץ PDF "מחקר" במצב explore) + regex-משבר "מה הטעם להמשיך/לחיות" (route.ts, אישור קליני ליה, lookahead חוסם תסכול-יומיומי) + qa-full max_tokens 250→1200. **פיצול scope מכוון: `lib/theorist-voices.ts` (ויניקוט edit-1+#1b) הוחזק בחוץ לריליס 2 — חסום על קריאה-עיוורת של ליה.** git: stash+rebase מעל 15 CCR commits (reports/memory, אפס חפיפת קוד), 920ee2b..78e1cb9. Post-deploy QA (איתן): יציב 3/4, safety 4/4 intercepted, RAG חי, אוגדן חזרתיות = jitter (BW-128, אורתוגונלי). BW-134+BW-135 → Done. **מועמד rollback: `78e1cb9`** (קודמו 920ee2b). דוח: release-reports/post-qa-2026-07-15.md.
- **12.07.2026 — Release fidelity-bundle (3 שינויי-קוד) → פרודקשן, יציב:** commit `152c1f0`, deploy `dpl_4xAcu82x5htRi9rngLHyEDhUXDVv` READY. הבאנדל = RAG register-gap paraphrase (`lib/query-paraphrase.ts` חדש) + safety CRISIS_REGEX_HE (route.ts) + wave-1 dedup (theorist-voices.ts). ה-tic-fix של ויניקוט **נמשך** אחרי ש-N=6 הראה רגרסיה. Post-deploy QA: 3/4 יציב, safety 4/4 intercepted, RAG חי, ויניקוט דגל-אורך יחיד (jitter). אישורים: ליה ✅ + איתן ✅ + איה go. **מועמד rollback: `152c1f0`** (קודמו 05769b2). BW-125 safety עדיין פתוח.
- **08.07.2026 — 2 תיקונים חיים בפרודקשן (crisis-gap + CORE_GUARDRAILS softening):** Fix A — `detectCrisis()` ב-route.ts קיבל את הפער "אין לי סיבה להמשיך" (+וריאציות) שנחשף בבדיקת פרודקשן (commit `63812de`, additive, BW-125 פתוח להעמקה dטרמיניסטית). Fix B — ריכוך header `CORE_GUARDRAILS` מ-"OVERRIDES STYLE" ל-"the floor beneath your register, not a replacement" + G9 לא-מחייב-hedging (commit `a16a10a`). **אימות איתן: QA חי 4 קולות (harness ישיר, /api/chat נעול-JWT) PASS נקי 6/6 כל קול** — הריכוך החזיר רגיסטר (הד-מילה + שהיית-החזקה + שאלה אחת) בלי להחליש בטיחות: בדייה עדיין נדחית (ספר בדוי + זיכרון מומצא נדחו בכל 4; ויניקוט אף תיקן ל-*Capacity to be Alone* 1958 האמיתי), עקיפה עדיין מתקבלת כחומר, אין צידוד נגד מטפל. אין regression, אין rollback. **מאמת את פסיקת ליה "build עובר ≠ קול מציית".**
- **07.07.2026 — Release CORE_GUARDRAILS גל 1 (G8/G9/G11):** בלוק גארדרילז אוניברסלי ברמת-פרומפט, נכתב פעם אחת ומוזרק לכל 4 הקולות דרך `staticSystem` (RAG-independent, נוכח בכל תור). commit `ad07ce7` — 2 קבצים by-name (theorist-voices.ts export CORE_GUARDRAILS + Hold-B anti-scripting; route.ts import + הצמדה ל-staticSystem), 43 insertions / 0 deletions (אדיטיבי טהור). **הליבה: staticSystem ולא dynamicSystem** — UNIVERSAL_SCOPE_INSTRUCTION הישן מוזרק ב-dynamic רק כש-RAG ריק (route.ts:471), נופל כש-RAG מצליח (המצב הרגיל ל-4 קולות). working tree מלוכלך (write-summary+sidebar sync+"לנתח") **הוחזק בחוץ מכוון** — write-summary לא QA חי, "לנתח" gated על BW-122. QA: איתן harness ישיר 60/60 נקי (8×4), G8/G9/G11 PASS. build 37/37. pull --rebase --autostash, push ea5b599..ad07ce7, dpl_FnRa2JNDV6nUCwxxpU5YgiS87PN5 READY ~26s. אישורים: איתן ✅ + איה go ("גו") + אדם לא נדרש (prompt additive, תקדים BW-94/96/97). מסמכי ריליס נדחפו בנפרד (commit ab94fb9). **ממתין ל-QA פרודקשן של איתן.** מועמד rollback: `ea5b599`. **דגל לאוליבר: 2 הערות קוד סטיליות "(G9 + G11)" ב-route.ts:418 + header theorist-voices → G8+G9+G11 (קוסמטי, קומיט נפרד).** אין Jira ייעודי (prompt-level additive). דוחות: release-reports/2026-07-07-2311.md + release-notes/2026-07-07.md + board-notes/release-2026-07-07.md.
- **05.07.2026 — 2 ריליסים סגורים באותו יום:** (1) BW-124 מד-האמת (`c6bbad2`, bucket B, 5 קבצים additive, secret-gated, dpl_2N4R3dW READY) + (2) פיצ'ר החזקה מטופל (`ba5e9fb`, buckets A+C, 6 קבצים 365/143, dpl_gqSQLB8 READY). הריליס השני יישר את פרודקשן ללוקאל במצב מטופל (זרימת picker תיאורטיקן → "שיחה עם [שם]"), פרט למצב מחקר שנשאר נעול-ללוקאל. **ה-HOLD מ-30.06 (5 עריכות מטפל/ת) נסגר** — הקוד הזה כבר בפרודקשן דרך הריליסים המאוחרים. **דגל פתוח: write-summary נדחף בלי QA מלא** — שכתוב קול ויניקוט, route חי (401) אבל איכות הקול לא אומתה בשיחה חיה. מומלץ smoke איתן על תוכן write-summary. מועמד rollback לריליס A+C: `7ff8351` (dpl_9jggd7n).
- Deployment אחרון: 14 יוני 2026 — QA scope fix (commit 3229688, dpl_2FEBk1ic READY ~33s): חסימת ורה+אליוט (frozen, נמשכו מפרוד) מלולאת specificTests ב-app/api/qa/route.ts — שורה אחת: `.filter(t => THEORISTS.includes(t))`. הם הופיעו ✅ בדוח היומי למרות שהוקפאו (מטעה). הגדרות הטסטים V1–V6/E1–E6 נשמרו במקום, frozen, מוכנות ל-unfreeze. אפס logic/voice. הקומיט גם תפס את release-reports/post-qa-2026-06-14-1403.md שהיה untracked. tsc EXIT=0. בקשת איה: "לדחוף עכשיו כדי שה-QA של מחר יהיה נקי". preflight: staged 2 קבצים by-name, pull --rebase --autostash (up to date), push e95eebe..3229688. אפס Jira (תיקון QA פנימי, אפס user impact). chat.getbetween.app חי.
- לפניו: Deployment 14 יוני 2026 — BW-94 קול אוגדן (commit e95eebe, dpl_FogeFB2C READY ~25s): שינוי prompt-only ב-lib/theorist-voices.ts בלוק אוגדן — סקשן VOICE IDENTITY (reverie not knowledge, tentative, never tell the patient who they are) + self-correction trigger כשהמטופל מסמן נחרצות. אפס logic/env/DB/route. אישורים: ליה ✅ (/eval PASS — הנחרצות מ-11.06 נסגרה, self-correction עובד, Tier 3 שייר לא חוסם) · איה go מפורש ("ממשיכים") · אדם לא נדרש (prompt fix תחום). preflight: staged רק 4 קבצי BW-94 by-name (theorist-voices.ts + eval-report + dev.md + lia.md), pull --rebase --autostash (crons הוסיפו eitan.md/board-notes/qa-analysis, ff 67f89c7→a9d247f), re-staged 3 הקבצים אחרי autostash, push a9d247f..e95eebe. Eitan post-prod QA: יציב, אין rollback — אוגדן נקי + safety 4/4 + אין regression מהשינוי; פרויד/קליין כשל timing בלבד (134/151ש) + ויניקוט שאלה כפולה (variance) — לא קשורים לבלוק אוגדן, למעקב ב-15.06. דוח post-qa-2026-06-14-1403.md. BW-94 → Done.
- לפניו: Deployment 14 יוני 2026 — security(deps) dependency vulns (commit f4c52ad, dpl_61N3 READY ~30s): צמצום פגיעויות npm 12→3. uuid+ws (npm audit fix non-breaking) · הסרת @xenova/transformers (לא בשימוש, אפס imports → מחק CRITICAL protobufjs RCE) · next 16.2.1→16.2.9 patch (מחק 9 advisories). package.json+package-lock.json בלבד, אפס קוד אפליקטיבי. אישורים: איתן ✅ smoke (release/eitan-approval.md) · איה ✅ go · אדם לא נדרש (patch פנימי, אפס user impact). preflight: commit יחיד לפני origin, pull --rebase --autostash (remote up-to-date), push 14a8757..f4c52ad. שני דומיינים 200. אין Jira (security פנימי). 3 moderate שנשארו לא חשופות (anthropic-sdk Memory Tool + postcss transitive בתוך next) — במעקב.
- לפניו: Deployment 13 יוני 2026 — BW-96/97 prompt fidelity (commit 29fe835, deploy READY ~23s): 2 כללים ב-lib/summary-prompt.ts (INTERPRETATION IS NOT FACT + SEPARATE WHO SAID WHAT) + 3 כללים + overall-logic ב-lib/supervise-prompt.ts (REGISTER/META-FEEDBACK/SOPHISTICATION). downstream agents בלבד — לא נגע בקולות התיאורטיקנים. אישורים: ליה ✅ (/eval, שער קליני) + איתן ✅ (smoke test, eitan-approval.md) + איה go מפורש (אדם לא נדרש). preflight: רק 2 קבצי lib staged by-name → stash(memory+mockups+reports+docs)→pull --rebase(72c5e14)→push→pop (conflict ב-eitan.md+maya.md, cron כתב במקביל, נפתר ידנית). BW-96+BW-97 → Done. Eitan post-prod QA: 3/4 + safety 4/4 יציב (פרויד 🔴 = BW-99 קיים, לא regression), commit d24d5e3.
- לפניו: Deployment 09 יוני 2026 — QA hotfix (commit d339d31, dpl_F2Uu READY ~25s): checkConsistency מדד word-count על therapist=slice(0,150) שנשמר רק לתצוגה → השטיח שונוּת אורך → דגל "תבנית קבועה" שקרי על ויניקוט/אוגדן (08.06). תיקון: TurnResult.therapistFull (לא נחתך) + checkConsistency מודד על הטקסט המלא עם [MEMORY:...] מוסר. tsc נקי. local 2 מאחורי origin (commits של agents בלבד) → commit→stash(memory+mockups+reports)→pull --rebase (נקי)→push→pop. שני הדומיינים 200. אפס Jira (תיקון QA פנימי, אפס user impact). go מפורש מאיה. אימות ידני של ה-QA נדחה לבוקר (cron ירוץ על הקוד המתוקן).
- לפניו: Deployment 08 יוני 2026 — UX+clinical batch (commit 001d7c2, dpl_Hsr7NX READY ~24s): פס הדר יחיד + תוויות תיאורטיקן + footer pills + כפתור intake על accent-deep + כלל קליני לקליין. אישורים: איתן ✅ ליה ✅ איה ✅ (אדם לא נדרש). release-note+board-note נדחפו (commit 1007280). תיעוד Jira retroactive (08.06 ערב, go מאיה): BW-78 (UX batch) + BW-79 (Klein clinical) נוצרו עם label release-2026-06-08 → Done. Eitan post-prod QA רץ על הפרודקשן: 2/4 (פרויד+קליין נקי, ויניקוט+אוגדן דגל "עקביות אורך" heuristic בלבד), safety 4/4, specificTests 6/6 → יציב עם WARNING, לא rollback. דוח: release-reports/post-qa-2026-06-08-2348.md.
- לפניו: 07 יוני 2026 — design refresh (commit b62b6c3, dpl_fBn6o1 READY): פלטת רוז עמוקה + פונט Assistant לעברית. BW-77 Done.
- לפניו: 07.06 security (commit eb01874, dpl_7Q3PFR READY) — service_role env var. BW-76 Done.
- לפניו: 02 יוני 2026 — Hold entry flow (commit 3055639, dpl_B9KKAE7 READY): BW-66/67/64/68, כולם Done ב-Jira
- Branch: main · remote: ayaavivi-design/psychoanalytic-space
- Vercel crons מוסיפים commits אוטומטית (QA reports) — remote יכול להיות קדים ל-local
- Jira MCP מחובר ועובד דרך cloudId: 03a5ff06-2b5e-41f3-ab73-14914bd6b3ca (ayaavivi.atlassian.net). Transition Done = id "41"
- ממתין: BW-69 (Maya UX, post-deploy non-blocker, פתוח)
- ממתין: BW-7–BW-15, BW-20 עדיין לא עודכנו ל-Done ב-Jira
- Post-deploy verification פתוח: BW-67 early-point ל-Freud/Klein/Ogden בשיחה חיה, מסך Hold ב-375/393/768, crisis path end-to-end כולל טקסט private

---

## Decisions & Gotchas
- **Jira = Atlassian Rovo בלבד**: השרת הפעיל הוא connector "Atlassian Rovo" — כלים `mcp__0bdec50d-5853-453a-bed0-10e2dfaa800c__*` (createJiraIssue/getJiraIssue/searchJiraIssuesUsingJql/createIssueLink/getVisibleJiraProjects/transition). cloudId `03a5ff06-2b5e-41f3-ab73-14914bd6b3ca` (ayaavivi.atlassian.net), Transition Done = id "41". **אל תשתמש ב-`mcp__atlassian__*` הישן** (mcp.atlassian.com/v1/sse) — מחזיר ריק/אין גישה. אם חיפוש חוזר `total: -1` או `[]` → אתה על השרת הלא נכון, עבור ל-Rovo. (יוני 2026)
- **תמיד `git pull` לפני `git push`**: Vercel crons יכולים להוסיף commit ל-remote בלי ידיעה.
- **Checklist שחרור**: Eitan sign-off + Oliver sign-off + founder sign-off — כולם חייבים לפני push.
- **לעולם לא `--no-verify`** אלא אם הוסכם מפורשות עם Oliver.
- **Jira לפני שחרור**: ודא שכל issues שנכנסים ל-release נמצאים בספרינט הנוכחי ומסומנים.
- **אל תדחוף ל-main בלי release note** — גם hotfix קטן מתועד.
- **working tree מזוהם = חסום שחרור**: לפני כל release בדוק `git status --short` + `git diff --stat`. אם קבצי ה-release מכילים hunks לא קשורים (עבודה uncommitted קודמת) — `git add <file>` ישגר הכל. אסור. או ניתוח כירורגי (`git add -p`) או החזקה עד שהעץ מאורגן. (גילוי 30.06.2026 — release מטפל/ת)
- **29 behind אבל all-cron ≠ סיכון build**: כש-local מאחורי origin, בדוק `git log HEAD..origin/main -- <app paths>`. אם ריק → כל ה-commits הם cron/agent (newsletters/QA/board), בסיס ה-build המקומי מייצג, ואין צורך rebase לפני אימות. (30.06.2026)
- **מצב מחקר (explore) נעול-ללוקאל בקוד**: `sb-explore-btn` ב-page.tsx עטוף ב-`{isLocalhost && ...}` (`window.location.hostname === 'localhost'`). לכן העלאת לוקאל→פרודקשן **אינה** חושפת מחקר — בפרודקשן הכפתור לא מרונדר, וה-HTML הנשלח לא מכיל `sb-explore-btn`. אותו דפוס עוטף גם פידבק-משתמש + חדר-הבורד. אימות: `curl prod | grep -c sb-explore-btn` = 0. (05.07.2026)

---

## History (last 10)
1. **Release 2 ויניקוט (edit-1+#1b) 15.07.2026** — שינוי-הקול שהוחזק מריליס 1 נדחף כקרקע אי-רגרסיה. commit `565a6dd`, deploy `dpl_9ShJh6Gq4VNZMpP7SkyPsMs1tMg1` READY production. **Winnicott-only** ב-`lib/theorist-voices.ts`, 41/56, כל ה-hunks 971-1313 (בתוך סקשן ויניקוט 909-1423). שורש #1 (FINAL CHECK "exactly one"→"AT MOST one, zero allowed, never add Q to landed turn") + #1b (symmetric-landing checklist) + #2 (משפטי-נחיתה מופשטים→זוג WRONG/RIGHT מדיאלוג) + dedup + stale-word. **שער: ליה חתמה אי-רגרסיה מפורשות — לא flow.** מדדים: 0/5 נחיתות (ידוע+מקובל, edit-1+#1b הכרחי-לא-מספיק, flow=גל CORE עתידי), maxQ=1, אפס ורדיקט/דליפה, safety intercepted. build EXIT=0, origin 0/0 בסנכרון → push נקי בלי rebase (78e1cb9..565a6dd). איה go ("יש אישור"). אין Jira ייעודי (prompt-level voice change). **מועמד rollback: `565a6dd`** (מועמד-חזרה 78e1cb9). חומר-גלם: eval-reports/rel2-winnicott-rawtext-for-lia-2026-07-15.md. (יולי 2026)
1. **Release 1 (UI+safety+QA) 15.07.2026** — פיצול scope: שלוש חתיכות לוקאליות ללא שער קליני נדחפו, שינוי-הקול (theorist-voices.ts ויניקוט) הוחזק לריליס 2 (חסום ליה). commit `78e1cb9`, deploy `dpl_AAe7jc5ANGhMvuJjG2Bpksmwb1cP` READY. תוכן: BW-135 (composer draft localStorage) + BW-134 (PDF filename "מחקר" ב-explore) + crisis-regex "מה הטעם להמשיך/לחיות" (route.ts, lookahead חוסם "להמשיך ל<פועל>" יומיומי, אישור ליה) + qa-full max_tokens 250→1200 (המייל היומי היה גדמים בלתי-בר-שיפוט לקריאה-עיוורת). QA standalone לרגקס: 8/8. git: push נדחה (15 CCR commits) → stash -u + rebase origin/main נקי + pop → push 920ee2b..78e1cb9. Post-prod QA (איתן): יציב 3/4, safety 4/4 intercepted, RAG חי, אוגדן חזרתיות jitter (BW-128). BW-134+BW-135 → Done. דוח: release-reports/post-qa-2026-07-15.md. (יולי 2026)
1. **Release fidelity-bundle 12.07.2026** — 3 שינויי-קוד נקיים בבאנדל אחד, commit `152c1f0`, deploy `dpl_4xAcu82x5htRi9rngLHyEDhUXDVv` READY production. (1) `lib/query-paraphrase.ts` חדש — RAG register-gap paraphrase (עברית מדוברת→אנגלית תיאורטית לפני embedding, fallback ל-raw query); (2) `app/api/chat/route.ts` — CRISIS_REGEX_HE ל-detectCrisis (סגירת מחלקת substring-evasion) + חיווט הפרפרז; (3) `lib/theorist-voices.ts` — wave-1 dedup (בלוק RESHAPE+TRACKING זהה-בייט הוסר מ-4 הקולות, קופל ל-CORE). **הפריט הרביעי (tic-fix ויניקוט) נמשך אחרי מדידה — N=6 הראה שהוא מרע כל מדד, ה-N=3 "ניצחון-חלש" היה רעש.** אישורים: ליה בלינד-read ✅ (wave1-milestone-lia) + N=6 של איתן. gotcha: working-tree עם 17 docs pre-staged + עשרות untracked → `git reset -q` ואז `git add` ל-3 קבצי-קוד בלבד; push נדחה (10 CCR commits) → `git rebase --autostash origin/main` נקי (05769b2→152c1f0). Post-deploy QA: יציב 3/4, **safety 4/4 intercepted**, RAG חי בכל הקולות, ויניקוט דגל-אורך יחיד (jitter, לא regression). דוח: release-reports/post-qa-2026-07-12-2339.md. BW-125 עדיין פתוח (item#2 cross-turn). (יולי 2026)
1. **Release BW-129 + BW-127 09.07.2026** — מיזוג כפתור "לנתח" (מטופל) → חוזה-העומק של analyze-note. 2 commits: `a19ef7f` (BW-129 — `/api/write-summary` נמחק וקופל ל-`/api/analyze-note` mode:'patient', פלט תלת-חלקי, חוזה graceful-hold חולץ ל-`lib/closure-contract.ts` משותף, `#sb-write-summary-btn` נמחק) + `ac507e2` (BW-127 redesign redirect בקולות). אישורים: ליה ✅ קלינית + איתן ✅ QA + איה go מפורש ("כן, deploy לפרודקשן"). **gotcha מרכזי — ahead-2/behind-6 מפחיד אבל אפס-סיכון:** origin היה 6 commits קדימה (כולם CCR reports/memory). אימתתי `git show --stat` לכל 6 → אפס חפיפת קוד; `git log HEAD..origin/main -- <app paths>` = ריק → **rebase נקי (לא merge)** של 2 הקומיטים מעל origin/main, ואז push `4f0062b..ac507e2`. stash-pop conflict רק ב-shaun.md (memory לוקאלי, לא בפוש) — נפתר ל-HEAD. **הכלל: תמיד `git log HEAD..origin/main -- <code paths>` לפני החלטת rebase/merge — CCR דוחף reports ל-main כל הזמן, behind-N נראה מפחיד אבל אפס סיכון build.** deploy dpl_Ei6y24voM6oYm6aHNXQo2te6apQv READY. Eitan post-prod QA: יציב 3/4 + safety 4/4 intercepted (פרויד חזרתיות = BW-128 ידוע, לא regression, אורתוגונלי). BW-129 → Done. דוח: release-reports/post-qa-2026-07-09-2151.md. (יולי 2026)
1. 2 תיקונים חיים 08.07.2026: Fix A crisis-gap ("אין לי סיבה להמשיך" ל-detectCrisis, `63812de`, BW-125 פתוח) + Fix B ריכוך header CORE_GUARDRAILS ("floor beneath register" במקום "OVERRIDES STYLE") + G9 לא-מחייב-hedging (`a16a10a`). אימות איתן harness חי 4 קולות PASS 6/6 כל אחד — רגיסטר חזר, בטיחות שרדה (בדייה נדחית, עקיפה=חומר). אין regression/rollback. (יולי 2026)
1. Release CORE_GUARDRAILS גל 1 07.07.2026: בלוק guardrails אוניברסלי ברמת-פרומפט (G8 NO COLLUSION + G9 NO FABRICATION + G11 OVERRIDE RESISTANCE) — נכתב פעם אחת, מוזרק ל-4 קולות דרך `staticSystem`. commit `ad07ce7`, 2 קבצים by-name, 43/0 (אדיטיבי טהור). **staticSystem בכוונה** (הרובד האוניברסלי הישן נופל כש-RAG מצליח). QA איתן harness 60/60 נקי, G8/G9/G11 PASS. build 37/37, push ea5b599..ad07ce7, dpl_FnRa2 READY ~26s. אישורים איתן ✅ + איה go + אדם לא נדרש. write-summary+sidebar+"לנתח" הוחזקו בחוץ מכוון. אין Jira ייעודי. מסמכי ריליס commit ab94fb9. דגל אוליבר: 2 הערות "(G9 + G11)" קוסמטיות. (יולי 2026)
1. Release החזקה מטופל (A+C) 05.07.2026: פרודקשן יושר ללוקאל במצב מטופל — זרימת picker תיאורטיקן (chips freud/klein/winnicott/ogden) → כפתור "שיחה עם [שם]" → `enterHoldConversation`. הכפתור הישן "שוחח עם תיאורטיקן" הוסר. 6 קבצים by-name (page.tsx/chat.js/globals.css/chat-route.ts/rag.ts/write-summary-route.ts), 365/143. הוראת איה: "תביא פרודקשן למצב לוקאל חוץ מפיצ'ר המחקר". **הבחנה: מחקר כבר נעול ב-`{isLocalhost}` → אין צורך לנתח קבצים, פשוט לא מרונדר בפרוד.** build נקי 37/37. push 7ff8351..ba5e9fb, dpl_gqSQLB8 READY 24.5s. smoke חי: chat.js holding present (10), כפתור ישן gone (0), דף 200, sb-explore-btn absent (0), write-summary 401. **דגל: write-summary לא עבר QA מלא.** דוח release-reports/2026-07-05-2100.md. (יולי 2026)
1. Release BW-124 מד-האמת 05.07.2026: bucket B בלבד (5 קבצים additive: fidelity route + rubric-prompt + fixtures + scenarios + FIDELITY-RUBRIC doc), secret-gated, on-demand (לא בקרון), אפס user impact. commit by-name `c6bbad2`, working tree מלוכלך → לא `git add -A`. pull --rebase --autostash מעל 10 cron commits, push b062c88..c6bbad2, dpl_2N4R3dW READY. אישורים: איתן smoke ✅ + איה go. post-deploy gating חי: בלי secret→401, שגוי→401, valid בלי theorist→400 (עבר gating, אפס עלות LLM). baseline לא הורץ (עלות, אישור נפרד). דוחות: release-reports/2026-07-05-1852.md + release/eitan-approval.md. Jira BW-124 לא נסגר (MCP בלי פרויקטים נגישים). (יולי 2026)
1. Hotfix vercel.json 17.06.2026: הסרת `_frozen_crons_BW114` מ-vercel.json — Vercel schema validation failure גרם ל-build failure בפרודקשן (commit 324ac0d). קובץ יחיד, tsc נקי. push c7e8ded..324ac0d ✅ · site חי. ללא Jira (ops fix). אישרה איה. (יוני 2026)
1. QA scope fix 14.06.2026: חסימת ורה+אליוט מ-specificTests ב-app/api/qa/route.ts (שורה אחת `.filter(t => THEORISTS.includes(t))`) — הם הופיעו ✅ בדוח היומי למרות frozen/נמשכו מפרוד. הגדרות V1–V6/E1–E6 נשארו, frozen ומוכנות ל-unfreeze. הקומיט תפס גם את post-qa-2026-06-14-1403.md (היה untracked). אפס logic/voice, tsc EXIT=0. בקשת איה לדחוף עכשיו ל-QA נקי מחר. preflight: 2 קבצים by-name, pull --rebase --autostash (up to date), push e95eebe..3229688, dpl_2FEBk1ic READY ~33s. אפס Jira (QA פנימי). (יוני 2026)
2. Release BW-94 14.06.2026: קול אוגדן (reverie מול נחרצות) — prompt-only ב-lib/theorist-voices.ts בלוק אוגדן: VOICE IDENTITY anchor + self-correction trigger (נוסחי ליה, הזרקת אוליבר). המראה ההפוך של BW-100. שער קליני: ליה /eval PASS (הנחרצות מ-11.06 נסגרה, self-correction נקי, Tier 3 שייר לא חוסם, eval-report ב-repo). go מאיה. preflight: staged רק 4 קבצי BW-94 by-name, pull --rebase --autostash (crons ff a9d247f), re-stage 3 modified אחרי autostash, push a9d247f..e95eebe. deploy READY ~25s · chat.getbetween.app חי · BW-94 → Done + הערת ריליס. Eitan post-prod QA: יציב, אין rollback — אוגדן נקי + safety 4/4, אין regression; פרויד/קליין timing בלבד + ויניקוט variance (לא קשורים לבלוק אוגדן, למעקב 15.06). דוח post-qa-2026-06-14-1403.md. **gotcha: ה-QA endpoint לא כותב קובץ QA-report ל-qa-reports/ — הפלט הוא ה-JSON מ-curl בלבד; הדוח האחרון בתיקייה הוא 13.06.** (יוני 2026)
3. Release BW-96/97 13.06.2026: prompt fidelity ל-downstream agents — 2 כללים ב-summary-prompt.ts + 3 כללים + overall-logic ב-supervise-prompt.ts (נוסחי ליה, הזרקת אוליבר). שער קליני: ליה /eval ✅ (supervise אוגדן pass→warn, control קליין נשאר pass/strong, summary לא מקדש פרשנות שנויה). שער QA: איתן smoke test ✅ (tsc EXIT=0 + runtime JSON דרך /eval). go מאיה. preflight: staged רק 2 קבצי lib by-name, stash לשאר → pull --rebase 72c5e14..29fe835 → push (pop נתן conflict ב-eitan.md+maya.md מ-cron מקביל, נפתר ידנית בלי איבוד). deploy READY ~23s · chat.getbetween.app חי · BW-96+BW-97 → Done. Eitan post-prod QA: 3/4+safety 4/4 יציב, פרויד 🔴 = BW-99 קיים (לא regression — הריליס לא נגע בקולות). דוח post-qa-2026-06-13-2337.md (commit d24d5e3). (יוני 2026)
4. QA hotfix 09.06.2026: checkConsistency ב-app/api/qa/route.ts מדד אורך/פתיחה על therapist=slice(0,150) (נשמר רק לטבלת הדוח) → השטיח שונוּת אמיתית → דגל "תבנית קבועה" שקרי על כל תיאורטיקן עם תגובות >150 תווים (ויניקוט+אוגדן 08.06). תיקון: TurnResult.therapistFull (לא נחתך) + checkConsistency מודד על הטקסט המלא עם stripMemory ל-[MEMORY:...]. slice לתצוגה לא השתנה. tsc נקי. local 2 מאחורי origin → commit b1d2309→stash(memory+mockups+reports)→pull --rebase→d339d31→push→pop. dpl_F2Uu READY ~25s · 2 דומיינים 200 · אפס Jira (QA פנימי). go מאיה. אימות ידני נדחה לבוקר. (יוני 2026)
