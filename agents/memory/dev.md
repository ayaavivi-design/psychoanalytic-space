# Oliver — Working Memory
_עדכן אוטומטית בסוף כל session. לא לערוך ידנית._

---

## Context
- Stack: Next.js 16 App Router · React 19 · chat.js (~5000 שורות vanilla JS)
- DB: Supabase — `knowledge_chunks` (RAG) · `user_conversations` (usage)
- Deploy: Vercel + native crons (vercel.json) · repo: ayaavivi-design/psychoanalytic-space
- Stripe: ON HOLD — ממתין לפתיחת חשבון
- Auth: Supabase Auth · guard `if (!supabaseClient)` לפני כל קריאת auth
- Input visibility: `data-bw-hidden` attribute (BW-38) — לא style.display

---

## Decisions & Gotchas
- **מצב פרודקשן↔לוקאל (15.07)**: ריליס 1 (UI+safety+QA, commit `78e1cb9`) שיגר את crisis-regex (route.ts) + qa-full max_tokens 1200 + BW-134/BW-135 (chat.js). **`lib/theorist-voices.ts` (ויניקוט edit-1+#1b, גל-3ב) נשאר LOCAL/uncommitted — הוחזק בכוונה מריליס 1, ממתין לריליס 2 חסום על קריאה-עיוורת של ליה.** ה-harness לא יכול לשמש שער (0/5 נחיתות תמיד); הזרימה תבוא מגל-CORE, לא מטלאי-דוגמה.
- **Jira = Atlassian Rovo בלבד**: השרת הפעיל הוא connector "Atlassian Rovo" — כלים `mcp__0bdec50d-5853-453a-bed0-10e2dfaa800c__*` (createJiraIssue/getJiraIssue/searchJiraIssuesUsingJql/createIssueLink/getVisibleJiraProjects). cloudId `03a5ff06-2b5e-41f3-ab73-14914bd6b3ca` (ayaavivi.atlassian.net). **אל תשתמש ב-`mcp__atlassian__*` הישן** (mcp.atlassian.com/v1/sse) — מחזיר ריק/אין גישה לפרויקט. אם חיפוש חוזר `total: -1` או `[]` → אתה על השרת הלא נכון, עבור ל-Rovo. (יוני 2026)
- **Supabase permissions**: תמיד `REVOKE`/`GRANT` מפורשים על טבלאות חדשות. אל תסמוך על ברירת מחדל — משתנה מאוקטובר 2026.
- **CCR → Vercel = 403**: CCR agents לא יכולים לקרוא ל-deployment URLs. crons חייבים להיות vercel-native (vercel.json).
- **supabaseClient guard**: תמיד `if (!supabaseClient)` לפני signIn/signUp/resetPassword — CDN לא תמיד נטען לפני הלחיצה.
- **updateReflectionBtn**: חייב להיקרא ב-4 מקומות — אחרי AI response, אחרי restoreConversation, ב-init, אחרי confirmTheoristEntry. חסרה קריאה אחת = bug.
- **restoreConversation**: חייב `updateSessionTitle(true)` אחרי `updateReflectionBtn()` — active-theorist-bar לא מתרענן בלי זה.
- **data-bw-hidden**: pattern לניהול visibility של auth-screen ו-flow elements — לא style.display.
- **input-area**: מוסתר ב-showModeSelect() + showTheoristEntry(), מוצג חזרה כש-chat מתחיל בפועל.
- **stream idle timeout**: ב-CCR — כתוב → שמור ל-/tmp → כתוב → שמור. לא לייצר הכל בתגובה אחת.
- **QA classification source-of-truth**: `lib/qa-fixer-map.ts` הוא המקור היחיד שממפה קוד issue → fixer בפרודקשן. qa-full מסווג כל issue ל-realFail (אין fixer → המשתמש רואה) או caught (יש fixer → caught≠clean אבל לא דלף). שמרני: קוד לא מוכר → fail.
- **QA drift guard**: `scripts/qa-drift-check.mjs` (npm run qa:drift) מאמת ששמות ה-fixers ב-QA_RULES קיימים ב-chat/route.ts, ושכל קוד ש-checkTurn פולט מסווג ב-QA_RULES. הרץ אחרי כל שינוי ל-checkTurn או ל-fixers. **לא** מחובר ל-build (אין test runner בפרויקט) — הרצה ידנית/cron.
- **fixers בפרודקשן הם local non-exported**: enforceOneQuestion/enforceVariedOpening/enforceSemanticRules ב-chat/route.ts אינם מיוצאים. המפה מסתמכת עליהם בשמם בלבד — לכן drift guard גרפי, לא import.
- **secrets בסקריפטי Python**: כל סקריפט ב-`scripts/` חייב לקרוא את ה-service_role מ-`os.environ["SUPABASE_SERVICE_ROLE_KEY"]` — לעולם לא hardcoded. הריפו ציבורי. ה-env var name הקנוני בכל הסטאק (lib/rag.ts, Edge Function, סקריפטים) = `SUPABASE_SERVICE_ROLE_KEY`. anon/publishable key (`sb_publishable_...` ב-chat.js) הוא ציבורי בכוונה — מוגן ע"י RLS, לא דליפה.
- **דליפת מפתח = rotation, לא edit**: עריכת קובץ לא מנקה את git history. אם service_role דלף — חייב Roll ב-Supabase דשבורד (זה הורג את הישן) + עדכון env ב-Vercel + redeploy. scrubbing של history אופציונלי אחרי rotation.
- **session-summary route גנרי — אין קול/RAG (BW-116)**: `/api/session-summary` מקבל רק שם תיאורטיקן כמחרוזת ומבקש "בגישה של X" — לא משתמש ב-`THEORIST_VOICE` ולא ב-RAG. הקול המלא + RAG חיים רק ב-chat route. כדי לקבל את הקול האמיתי בסיכום/ניתוח צריך להזריק `THEORIST_VOICE[t]` + `searchKnowledgeHybrid` ידנית — כמו שעשיתי ב-`/api/analyze-note`.
- **state של ניתוח inline (BW-116)**: `שמור` מנקה את dailyText — אבל אסור להשאיר את ניתוח ה-draft יתום. saveDailyUpdate חייב להעביר `noteAnalysis['draft']` ל-id החדש + `delete next['draft']`, ולשמור את הניתוח כשדה `analysis` על הפתק ב-localStorage. analyzeNote על עדכון שמור (key≠'draft') שומר גם הוא; loadCaseUpdates עושה hydrate. **timing gotcha:** שמירה לפני שה-fetch של "נתח" חזר → אין מה להצמיד, היתום חוזר.
- **מד-אמת = route נפרד, לא הרחבת judge-full**: `/api/fidelity` חיובי (0-100 קרבה), judge-full שלילי (ספירת הפרות). **בכוונה נפרדים** — לא לערבב, כדי לא לסכן regression ל-QA היומי בפרודקשן. scenarios נפרדים (`fidelity-scenarios.ts` 6 תורות מול judge-full 3) — מימד 2 (נחיתה) אינו תקף מתחת ל-5 תורות (החומר טרם הבשיל, החזקה נכונה תקבל 0 שלא בצדק). ה-scoring prompt אגנוסטי-לשפה (מנקד transcript בכל שפה) אבל התרחישים עברית בלבד כרגע. משקלול בקוד לא במודל. הרצה עולה → אישור איה לפני baseline.
- **consult chat ↔ תצוגת React של מטפלת (BW-116)**: `startConsultation` (page.tsx) מסתיר `#welcome` (`display:none`) ומזריק טקסט ל-`#user-input` + `sendMessage()`. שני באגים שנבעו מזה: (1) **ניווט בסייד-בר** (המקרים שלי/ארכיון) מתוך צ'אט פעיל לא הציג כלום — תצוגות המטפלת חיות בתוך `#welcome` שנשאר מוסתר. פתרון: `window.bwExitChatToHome()` (chat.js) מנקה שיחה ומחזיר `#welcome` בלי `showModeSelect()` (שמתאים רק למטופל ומקלקל את תצוגת ה-React). (2) **שכפול טקסט**: `sendMessage` מנקה את ה-input רק ב-~6111 *אחרי* `await checkConversationLimit()` (הודעה ראשונה) — אז ה-seed נשאר בשדה. פתרון: לנקות `input.value=''` ב-startConsultation מיד אחרי הקריאה (sendMessage תופס את הטקסט סינכרונית בתחילתו, לפני ה-await).
- **shared closure-contract = lib נפרד, לא שכפול (BW-129)**: מנגנון ה-BW-126 (endsOnQuestion/attemptFirstPersonLanding/forceFirstPersonLanding) חולץ מ-`app/api/write-summary/route.ts` ל-`lib/closure-contract.ts` (+`enforceClosureContract()` wrapper) **לפני** מיזוג הכלים — כדי שכל route שצריך hold-safety יקרא לפונקציה אחת, לא יכפול לוגיקה. write-summary route נמחק אחרי המיזוג; analyze-note הוא היחיד שקורא ל-lib הזה כרגע. **כלל לעתיד:** כל route חדש שצריך graceful-hold → ייבא מ-`lib/closure-contract.ts`, לא ייצור העתק.
- **QA harness שקורא source של route ישירות = שביר למחיקת/שינוי route**: `scripts/*.mjs` שמחלצים prompt חי דרך `fs.readFileSync(route.ts)` (כי אין tsx/ts-node בפרויקט לייבוא ישיר) נשברים לצמיתות אם ה-route נמחק/עובר. קרה בפועל: מיזוג BW-129 מחק את `write-summary/route.ts` → `eitan-write-summary-held-qa.mjs` (untracked) הפך לבלתי-ניתן-להרצה, נמחק והוחלף ב-`eitan-analyze-note-merged-qa.mjs`. **כלל לעתיד:** לפני מחיקת/מיזוג route — לחפש `grep -rl "route-name" scripts/` ולעדכן/למחוק סקריפטים תלויים כחלק מאותו שינוי, לא בנפרד.
- **`.next/types` stale אחרי מחיקת route**: מחיקת API route משאירה רפרנס ב-`.next/types/validator.ts` שגורם ל-`tsc --noEmit` להיכשל על מודול לא-קיים עד `rm -rf .next` (או build מלא). לא regression אמיתי — false positive של cache. בדוק עם `rm -rf .next && npx tsc --noEmit` לפני שמסיקים כשל אמיתי.
- **פער-רגיסטר — הפתרון = פרפרז, לא רק הרחבה (12.07, נמדד)**: איתן מדד — (א) הרחבת-הקשר לבד **לא חצתה 0.59** (T4 ויניקוט: single 0.377 → cum-3 0.523, שיפור אבל תת-רצפה). הפתרון שעבד = **שכבה 1(ב): פרפרז Haiku→אנגלית תיאורטית לפני embedding** (`lib/query-paraphrase.ts` → `paraphraseForRetrieval(anthropic, rawQuery)`, מוזרק ב-`route.ts` בין בניית ה-query ל-`searchKnowledgeHybrid`). מודד: T4 ויניקוט → **0.630 ✅**, T3–T6 חוצים בכל 4 הקולות. **gotchas:** הפרפרז חייב תיאורי לא-מאבחן (קו-אדום CORE), חולף (query בלבד, לא נראה למטופל/קול), fallback ל-rawQuery בכשל (לעולם לא חוסם). T1/T2 עדיין תת-רצפה בחלק מהקולות — תורי-פתיחה דלילים, מתייצב מ-T3. מודל `claude-haiku-4-5`, עלות +קריאת-LLM אחת על הנתיב-החם (~300-800ms).
- **RAG query = הרחבת-הקשר, לא תור-בודד (פער-רגיסטר, 12.07)**: `route.ts` בנה את ה-query לשליפה מ-`lastUserMessage` בלבד — משפט עברי-מדובר קצר שנשלף **מתחת לרצפת 0.59** מול הקורפוס התיאורטי-אנגלי (probe של ליה: השאלה הכי חיה = 0.34–0.39). זה **פער-רגיסטר סיסטמי בכל 4 הקולות**, קבוע (לא BW-131 הלסירוגין). התיקון (א, הכי זול/הפיך): ה-query = 3 תורות-המטופל האחרונים מצטברים (`recentUserTurns.slice(-3)`), **לא** תורי-התיאורטיקן (מונע קרקוע-על-פלט-עצמו). tsc נקי. **עוזר רק ברב-תורי** — probe-משפט-בודד לא ירגיש הבדל. אם לא חוצה 0.59 → שלב-ב: פרפרזת-LLM לרגיסטר תיאורטי לפני embedding (יקר יותר, +קריאת-רשת). hybrid (BM25) חי ב-DB אבל כמעט חסר-ערך לפער עברי↔אנגלי. הבא: איתן מודד before/after מול המילים האמיתיות של איה.
- **BW-129 QA finding — נתיב ה-held לא נורה בפועל בהרצה הראשונה**: אותו טקסט טעון שהצית held-path 6/6 ב-write-summary (BW-126) הצליח להידחס ל-JSON תלת-שדות 12/12 פעמים במאוחד (`analyze-note`, patient mode) — כולל שני התרחישים ה"טעונים" במפורש. המנגנון הדו-שכבתי קיים ומחווט (`enforceClosureContract`), אבל **לא נבחן חי על מקרה שבאמת holds** תחת הפרומפט המאוחד. bring_to_session בכל 12 המקרים גוף-ראשון, לא שאלה — תקין ל-AC#3/5. **דגל לאיתן/ליה:** אם רוצים הוכחת-אש אמיתית לנתיב ה-held במאוחד — צריך חומר טעון יותר קיצוני (או שיחה רב-תורית) כדי להכריח סירוב-JSON.

---

## History (last 10)
1. **feat(mobile) — שלב 2: תפריט אווטאר + פוליש-טוקן, לוקאל, uncommitted (24.07.2026).** לפי פסק מאיה. `globals.css`: טוקן `--layout-header-mobile:56px`; `.header-top` מובייל min-height+`padding:0 md 0 lg`; `.bw-header-avatar .sb-avatar`→36px override (סייד-בר נשאר 30); סגנונות `.bw-acct-account-row`/`.bw-acct-name/email`/`.bw-acct-lang`(pill HE/EN). `page.tsx`: מבנה-מחדש `#bw-account-menu` — שורת-חשבון(אווטאר+שם+אימייל) · מתג-שפה · | · הגדרות/התנתק · | · [מטפל:חיפוש/סיכום]+PDF(gated)+intake(Sparkles, patient). `chat.js`: `toggleAccountMenu` ממלא שם/אימייל בשורת-החשבון בזמן-פתיחה מ-`#sb-user-name/email` (בלי לשכפל ID). **PDF נשאר בתפריט gated עד שלב 3 (סאב-הדר שיחה) — לא מאבדים גישה.** tsc+lint+syntax נקי, 3 קבצים, דסקטופ/קופי לא זזו. **לוקאלי, לא נדחף.** דגל: intake=Sparkles (בחירת-אייקון שלב-2, מאיה תאשר).
1. **feat(mobile header) — שלב 1 עיצוב Claude Design, לוקאל, uncommitted (24.07.2026).** תיקנתי את הכותרת השבורה (`f085b69`, "Between" נחתך). `page.tsx`: import `MessageCirclePlus`; class `header-left` לאשכול-שמאל; הסרתי ✎ (PenLine) מהשמאל; הוספתי כפתור בועה (`MessageCirclePlus size=21 strokeWidth=1.75 color=--text radius=--radius-md hover=--accent-soft`) לקבוצת-ימין לפני האווטאר. `globals.css` @media≤600: `.header-left{display:none}` + `#header-intake-btn{display:none!important}` + הסרת `order:2` מ-h1 (עוגן שמאל, שלם). **תוצאה @375: ~223px (Between שלם, לא נחתך). דסקטופ לא זז, קופי לא זז.** tsc+lint:tokens נקי. **🚩 דגלי-טוקן למאיה (העיצוב משתמש בערכים שלא בטוקנים):** wordmark 27px (השתמשתי --fs-heading-xl=26, δ+1), header height 56px (לא הוגדר, padding=--space-md), padding design 0/10/0/18 (השתמשתי --space-md=12), avatar 36px (נשאר .sb-avatar=30). המבנה נכון, הפיקסל-פוליש ממתין להחלטת מאיה (טוקנים חדשים או מיפוי). **לא נדחף.** שלב 2 = תוכן תפריט האווטאר (שפה/intake/תיאורטיקנים) + כרטיס כתיבה.
1. **feat(clinic-panel skeleton) — endpoint מולטי-מודלי, לוקאל בלבד, uncommitted (24.07.2026).** בניתי `app/api/clinic-panel/route.ts` (שלד, ~140 שורות): POST מגודר-secret (`?secret=`=`QA_SECRET`, כמו qa-full), מקבל `{material}`, קורא 3 פרומטי-פרסונה מ-`agents/*.md` (fs), מריץ 3 קריאות ב-Promise.all **כל אחת על מנוע אחר** — ליה→Anthropic (`claude-sonnet-4-6`), ורה→**OpenAI** (`gpt-4o`, אחרי שאיה הכניסה מפתח), אליוט→**Gemini** (fetch ישיר ל-REST, בלי dependency, `GEMINI_MODEL`||`gemini-2.5-flash`) — ואז סינתזה (ליה→Anthropic). config map `PANEL` להחלפת ניתוב בשורה אחת. 3 מתאמים: `callAnthropic`/`callGemini`/`callOpenAI`(stub). `tsc --noEmit` נקי. **המטרה: לשבור תיאום-מודל → פאנל מהוכחה ולא נורת-אזהרה.** env: `GEMINI_API_KEY` (איה מוסיפה ל-.env.local), `ANTHROPIC_API_KEY`+`QA_SECRET` קיימים. **stubbed/מאוחר:** OpenAI (ורה), שליפת-חומר-אוטומטית, כתיבה-לקובץ+commit, cron. **⚠️ Vercel gotcha:** קורא `agents/*.md` דרך fs ב-runtime — עובד לוקאלית, ל-Vercel צריך bundling (`outputFileTracingIncludes`) לפני deploy. לא נדחף. **✅ נבדק חי (24.07): פאנל תלת-ספקי מלא עובד** על שיחת קליין אמיתית — Claude+GPT-4o+Gemini רצו, שלושתם ייחסו קליין, מחלוקת אבחנתית (ורה/GPT=Tier3 חזרתיות, ליה/Claude=Tier2 reassurance). **3 gotchas שנתפסו: (1) `gemini-2.5-flash*` מחזיר 404 "no longer available to new users" למרות שמופיע ב-ListModels → השתמש `gemini-flash-latest` (נבדק, עונה); (2) OpenAI מחזיר 429 insufficient_quota עד שמסדרים billing בחשבון (לא באג קוד — פעולת חשבון); (3) synthTask חייב לבנות מיפוי-מנועים דינמית מ-PANEL — hardcode מתיישן ומטעה את הסינתזה על עצמאות.**
1. **fix(winnicott גל-3ב — שורש #1 + שורש #2) — לוקאל בלבד, uncommitted (14.07.2026).** אחרי החקירה היסודית של איתן+ליה על קוד ויניקוט (`eval-reports/winnicott-code-audit-eitan-lia-2026-07-14.md`), תיקנתי 3 שורשים ב-`lib/theorist-voices.ts`: (1) **שורש #1 (הסתירה שמייצרת "לא זורם")** — FINAL CHECK #1 (שורה ~1289) `"exactly one"`→`"AT MOST one. Zero is allowed and is often the stronger move... Never add a question to a turn that already landed"`. זה מרים את הנחיתה לרמת-האכיפה של השאלה (עקבי עם ורה 2692/אליוט 2882 שכבר אומרים "or zero"). (2) **שורש #2 (המשפך)** — החלפתי 3 משפטי-נחיתה מופשטים/תלושים (שורות 979-982) בזוג WRONG/RIGHT מתוך דיאלוג מודגם (אמא/גאה) שהמודל יכול לייצר ממנו נחיתה. (3) **dedup** — הסרתי `"Do not name the emotion for them"` משורה 1116 (שוכפל בקיפול name-emotion 1070-1073 + final-check 1302b) + תיקון מילה stale `כורם`→`מכווץ` בשורה 1302 (שריד קיפול-3 מהסשן הקודם). **מדידת איתן (N=2, `eitan-gal3-qa.mjs`): 0/5 נחיתות בחומר לא-מודגם, 5/5 שאלות — הרפלקס שלם.** רצפה מושלמת (maxQ=1, 0 ורדיקט, 0 דליפת-זהות, בטיחות intercepted, אין פספוס-מהופך). **הלמידה הארכיטקטונית: תיקון שורש #1 הוא תנאי הכרחי אך לא מספיק לזרימה; דוגמה חיובית בודדת לא מנצחת את המשפך של ~20 איסורי-MANDATORY.** קרה כאן גם **teaching-to-the-test**: הגרסה הראשונה של דוגמת שורש #2 נבנתה על אותו חומר שההארנס מודד בתור-5 → המודל העתיק מילה-במילה = ירוק מזויף; החלפתי לחומר לא-בהארנס וה-0/5 האמיתי נחשף. **הכיוון האמיתי לזרימה: גל-CORE (קיפול הצ'קליסט בן 19 + הורדת ריבוי-האיסורים), לא עוד טלאי-דוגמה.** הכל LOCAL. הבא: ליה קריאה עיוורת על הטקסט הגולמי → תכנון גל-CORE.
1. **release(fidelity-bundle) — נדחף לפרודקשן, commit `152c1f0` (12.07.2026).** 3 שינויי-הקוד הלוקאליים (שתי הרשומות למטה + wave-1 dedup) קובצו לריליס אחד וסם דחף. **הבאנדל:** (1) `lib/query-paraphrase.ts` (RAG register-gap paraphrase); (2) `app/api/chat/route.ts` (CRISIS_REGEX_HE + חיווט הפרפרז); (3) `lib/theorist-voices.ts` (wave-1 dedup — בלוק RESHAPE+TRACKING זהה-בייט הוסר מ-4 הקולות, קופל ל-CORE). **הפריט שנמשך: tic-fix של ויניקוט.** ניסיתי לתקן את הטיק המכני (הד—מקף—שאלה) של ויניקוט; איתן מדד N=6 → **הרעה בכל מדד** (endsOnQuestion 63→70%, landings 37→23%, ticShape 30→53%). **הלמידה הארכיטקטונית: כלל שלילי ב-FINAL CHECK ששם את התבנית מפורשות מזמין אותה (priming) — התיקון הנכון = הדגמה חיובית (WRONG/RIGHT exemplars) בגוף-הקול, לא עוד MANDATORY. גל עתידי מגודר: ליה מגדירה יעד → אני מדגים → N=6+בלינד-read.** post-deploy QA: יציב 3/4, safety 4/4, RAG חי. dpl_4xAcu82x5htRi9rngLHyEDhUXDVv READY.
1. **feat(RAG paraphrase, פער-רגיסטר שכבה-1 (ב)) — לוקאל בלבד, uncommitted (12.07.2026).** אחרי שאיתן מדד ש-(א) לבד לא חוצה 0.59, איה אישרה (ב): Haiku+אנגלית. **מה בוצע:** `lib/query-paraphrase.ts` חדש — `paraphraseForRetrieval(anthropic, query)` שמתרגם את 3 תורות-המטופל העברית ל-2-3 משפטי אנגלית תיאורטית נייטרלית לפני embedding (מודל `claude-haiku-4-5`, max_tokens 150). מוזרק ב-`route.ts` בין בניית ה-query ל-`searchKnowledgeHybrid`, + import. **גבולות:** תיאורי לא-מאבחן (קו-אדום CORE), חולף (רק query, לא נראה), fallback ל-rawQuery בכשל (try/catch פנימי — לעולם לא חוסם). `tsc --noEmit` נקי. **מדידת איתן (probe (ב) על תורי-איה מה-PDF):** T4 ויניקוט 0.377→**0.630 ✅**, T3–T6 חוצים בכל 4 הקולות; T1/T2 עדיין תת-רצפה (תורי-פתיחה דלילים, מתייצב מ-T3). לא נדחף. **הבא: איה חוזרת לויניקוט חי → [RAG] logs חוצים 0.59? הטיק המכני (הד-מקף-שאלה) נעלם?** אם הטיק שורד למרות קרקוע → שכבה-2 (קול, `theorist-voices.ts`).
1. **fix(RAG query, פער-רגיסטר שכבה-1 (א)) — לוקאל בלבד, uncommitted (12.07.2026).** אבחנת ליה (`eval-reports/rag-register-gap-diagnosis-lia-2026-07-12.md`): שיחה חיה של איה עם ויניקוט השתטחה — RAG שולף את המילים האמיתיות שלה **מתחת ל-0.59** בכל 4 הקולות (query = תור-בודד עברי-מדובר מול קורפוס תיאורטי-אנגלי). איה: "ממשיכים עד שהם מתייצבים" + "תמשיך" (האצילה החלטה). **מה בוצע ב-`app/api/chat/route.ts` (473–477→484):** ה-query הורחב מ-`lastUserMessage.content` בלבד ל-`recentUserTurns.slice(-3)` — 3 תורות-המטופל האחרונים מצטברים, join ב-`\n`, דרך `getMsgText` helper (מטפל ב-string|array content). **רק תורי-מטופל** (לא תיאורטיקן — מונע קרקוע-על-פלט-עצמו). שינוי מחרוזת בלבד, אפס latency/טוקנים/route-flow. הפיך (revert ל-lastUserMessage). `npx tsc --noEmit` נקי (exit 0). **עוזר רק ברב-תורי.** לא נדחף. **הבא: איתן מודד** — probe רב-תורי על תורי-השיחה של איה מה-PDF (cumulative-3 מול single), רף לחצות 0.59; + איה חוזרת לויניקוט חי → [RAG] logs חוצים 0.59? הטיק נעלם? אם לא חוצה → שלב-ב (פרפרזה).
1. **תיאור ארכיטקטוני — פירוק ערימת-הגארדרילז (שולחן עגול fidelity, 11.07.2026, לא קוד).** ליה הכריעה קלינית (3 דליים 🔴/🎭/🟡), אני נתתי היתכנות טכנית. **אימותים מול הקוד:** (1) route.ts:424 — `staticSystem = ...THEORIST_VOICE[theorist] + ... + CORE_GUARDRAILS`; CORE (שורה 3080, G8/G9/G11/G12) מוזרק פעם אחת בזנב, ב-cache. **ה-vehicle לאיחוד כבר קיים.** (2) 4 קולות פעילים: freud(2)/klein(460)/winnicott(911)/ogden(1426); loewald/bion/kohut/heimann=legacy רדום. (3) **בלוק RESHAPE+TRACKING+ALIVENESS+RESISTANCE זהה מילולית** בין הקולות (diff Freud≡Klein IDENTICAL 34 שורות) — ~128 שורות×4, refactor טהור build-safe. (4) ויניקוט: 27 שורות WRONG/RIGHT (~13 זוגות) + item 11 INVASION CHECK (1418, 5 תת-בדיקות a-e) — הקיפול = לשמור הכלל, לקפל הדגמות לבלוק-דוגמאות אחד. **דגלים:** (א) הזזת 🟡 מאמצע-מחרוזת ל-CORE-בזנב משנה מיקום-בפרומפט = סיכון fidelity שלא נתפס ב-build → מדידת איתן before/after; (ב) RESHAPE שכן ל-G8 NO COLLUSION — ליה לאמת אין כפילות; (ג) legacy bion יש לו RESHAPE inline (2175) → יכפיל אם יופעל, להחליט לנקות/לתעד; (ד) מדידה: `wc -l` פר-בלוק + build 36/36 שער כל גל. **הרצפה (🔴 SAFETY_PROTOCOL 3046 + G1 route + מגדר/שפה) מנותקת פיזית מהערימה → נעולה בלי לגעת.** לא קוד עד אישור איה. הבא: איתן (מדידה) → הילי (רצף).
1. **feat(גל-נחיתה fidelity — קליין 8b + פרויד 8b) — לוקאל בלבד, uncommitted (11.07.2026).** גל ב׳ פריט נחיתה, אנלוגי ל-BW-132/גל א׳. ליה חתמה על הנוסח, איה אישרה ("ממשיכים"). **הרקע:** ערימת דיכוי-פרשנות (G8+G9(4)+G13) חונקת קולות-נחיתה — פרויד מפסיק לפרש, קליין מפסיקה לנחות. **מה בוצע ב-`lib/theorist-voices.ts` (הזרקת item יחיד לכל קול, אפס לוגיקה):** (1) **פרויד 8b** "STATEMENT REQUIREMENT — Freud interprets, he does not only ask" — הוזרק בין item 8 (QUESTION RHYTHM, 456) ל-item 9 (IDENTITY); מחייב נחיתה-אמירה מהתור השלישי, ארכיאולוג נוקב בקבור, סעיף-הזהב מולחם (לא perhaps, אבל לא וורדיקט-מיישב → G13/G9 בתוקף). (2) **קליין 8b** "LANDING — Klein leaves the interpretation standing" — הוזרק בין item 8 (DO NOT ASK AND ANSWER, 906) ל-item 9; סיום בנקודה מהתור השלישי (splitting/PI/envy בביטחון-קליין), לא מחליש item 1/item 8. **אימות מספרים בעצמי (grep, לא מהזיכרון):** פרויד item8=456 (לא "item 10" כמו שהזיכרון הישן טען!), קליין item8=906. תקדים "Nb" קיים (0b/5b/3b) → אפס מספור-מחדש. escaping: גרשיים בודדים/כפולים בתוך backtick — אין צורך ב-escaping (רק backtick/`${` היו דורשים, ואין). **מיקום per-voice (THEORIST_VOICE), לא CORE — אפס שינוי route.ts.** `npm run build` נקי (✓ Compiled, 36/36). **הנחיתה סמנטית → build לא תופס, חייב QA חי של איתן** (per-voice N≥3, יעד: מהתור השלישי סיום בנקודה, בלי regression על ONE QUESTION/IDENTITY/safety). לא נדחף. הבא: איתן QA חי → חתימת ליה על RAG בריא.
1. **feat(BW-129, מיזוג write-summary→analyze-note) — לוקאל בלבד, uncommitted (09.07.2026).** תדרוך+אישור מלא של איה ("מסכימה לגביי לא לשכפל" + "מאשרת את התוכנית"). **מה בוצע:** (1) `lib/closure-contract.ts` חדש — חילוץ מנגנון BW-126 הדו-שכבתי (endsOnQuestion/attemptFirstPersonLanding/forceFirstPersonLanding/enforceClosureContract) מ-write-summary, כדי שלא ישוכפל. (2) `lib/analyze-note-prompt.ts` — פוצל ל-FORMAT PATIENT (3 שדות חדשים: what_came_up/core_insight/bring_to_session) ו-FORMAT THERAPIST (5 השדות המקוריים, **ללא שינוי**), נוסף סעיף held-path (פרוזה, לא שאלה) לממשק המטופל בלבד. (3) `app/api/analyze-note/route.ts` — קורא ל-`enforceClosureContract` בנתיב המטופל כש-JSON נכשל; ממשק המטפל נשאר 500-on-parse-failure **ללא שינוי** (BW-116 לא נוגע). (4) `app/api/write-summary/route.ts` **נמחק לגמרי** (+התיקייה). (5) `public/chat.js` — `openWriteSummary()` מצביע ל-`/api/analyze-note` עם `mode:'patient'`+gender; רינדור לא-held בנוי משלושת השדות החדשים; `openWriteArchive()` תואם-לאחור לשלוש צורות היסטוריות (key_points ישן / 3-שדות חדש / held-reflection); הוסרו 3 בלוקי dead-code שהתייחסו ל-`sb-write-summary-btn`+hint מיושן, hint עודכן להפנות לכפתור "לנתח" תחת הכתיבה. (6) `app/page.tsx` — נמחק בלוק הכפתור המוסתר `sb-write-summary-btn` + import `NotebookPen` הפך מיותם ונמחק; כפתור "לנתח" הפעיל **לא זז ולא שונה** (AC#1). **אימות:** `npm run build` נקי (37/37, route list מאשר analyze-note קיים/write-summary נעלם), `npm run lint:tokens` נקי, `rm -rf .next && tsc --noEmit` נקי (false-positive stale-cache טופל). **QA חי (הרנס ישיר, לא build)** — `scripts/eitan-analyze-note-merged-qa.mjs` חדש (5 תרחישים × N=3, דוח: `eval-reports/eitan-analyze-note-merged-qa-2026-07-09-06-59.md`): 15/15 הרצות החזירו JSON תקין; 0/12 bring_to_session בממשק המטופל התנסח כשאלה; therapist regression 3/3 עדיין 5-שדות מקוריים ללא שינוי. **ממצא לדגל:** 0/12 הרצות (כולל 2 התרחישים ה"טעונים" במפורש) פגעו בנתיב ה-held בפועל — המנגנון קיים ומחווט אבל לא נורה חי במאוחד הזה (ראה Decisions & Gotchas). סקריפט ה-QA הישן (`eitan-write-summary-held-qa.mjs`) נמחק — קרא source של route שנמחק, בלתי-ניתן-להרצה יותר, היה untracked. **לא נדחף.** נותרו חסומים לפני Done ב-Jira: AC#7 (QA חי של איתן בעצמו — יש כרגע רק את שלי) + AC#8 (סימוכין קליני של ליה שהפלט לא שובר קווים אדומים).
1. **fix(write-summary, אכיפה דו-שכבתית לנתיב ה-held) — לוקאל בלבד, uncommitted (08.07.2026), מחליף/מתקן את הפיצ'ר-פרומפט-בלבד מתחת.** איתן הריץ QA חי על פיצ'ר-הפרומפט-בלבד (הרשומה הקודמת) — **FAIL מוחלט: 6/6 held-path outputs עדיין הסתיימו בשאלה** (BW-126). מסקנה: הוראת-טקסט בודדת בסוף פרומפט ארוך לא יכולה לגבור על כללי-קול שמוזרקים מוקדם יותר וחזקים יותר ("ONE QUESTION ONLY"). איה אישרה ("כן") מנגנון דו-שכבתי שלא תלוי בציות המודל. **מה תוקן ב-`app/api/write-summary/route.ts`:** 3 פונקציות עזר מעל `POST` — `endsOnQuestion` (בודקת `?` בסוף), `attemptFirstPersonLanding` (ניסיון תיקון טבעי — קריאת Anthropic נוספת קצרה שמנסחת מחדש רק את המשפט/פסקה האחרונה כנחיתה בגוף-ראשון, לא נוגעת בשאר הטקסט; best-effort, נכשלת בשקט לטקסט המקורי), `forceFirstPersonLanding` (רשת ביטחון דטרמיניסטית — קוד, לא מודל: מזהה עברית/אנגלית, מחליפה פסקה/משפט אחרון שמסתיים בשאלה בנחיתה קבועה: "אני רוצה להביא את זה לפגישה." / "I want to bring this to the session."). **wiring בזנב `POST`:** `reflection` נבדק ל-`?`→אם כן מנסה תיקון טבעי→נבדק שוב→אם עדיין `?` נכפית הנחיתה הדטרמיניסטית. הבטחה: אפס פלטי-שאלה יכולים לצאת מנתיב ה-held, ללא תלות בציות המודל. `npx tsc --noEmit` נקי (אפס שגיאות). געתי רק בקובץ הזה — אפס שינוי ל-JSON parsing הרגיל / UI / ניתוב. **דורש אימות חי מחדש (לא build) — איתן להריץ שוב את `scripts/eitan-write-summary-held-qa.mjs`, יעד 0/N מסתיימים בשאלה** (במקום 6/6). תרחישי בדיקה שהצעתי לאיתן: (1) חומר טעון קלאסי HE, (2) אותו דבר EN (לוודא זיהוי-שפה נכון ברשת הביטחון, לא "מדביק" נחיתה עברית לטקסט אנגלי), (3) reflection בפסקה אחת (לוודא שנתיב ה-fallback הרגקסי, לא רק פיצול-פסקאות, עובד). לא נדחף. הבא: איתן QA חוזר → אם PASS, ליה מאמתת קלינית 2-3 פלטים חיים לפני חשיפה.
1. **fix(write-summary, חוזה-סגירה לנתיב ה-held) — לוקאל בלבד, uncommitted (08.07.2026).** הילי הכריעה: סקופ א/ב (ניתוב מול עיצוב-מחדש) **מוקפא** עד סגירת הסדק+שער שלב 0 — אבל התיקון הקליני עצמאי מהסקופ (ליה, `bizdev/WRITE-SUMMARY-DEPTH-RECOMMENDATION-2026-07-08.md` סעיף 3ב: "תקף גם אם בוחרים (א) וגם (ב)") אושר לביצוע מיידי כחריג-כמעט-אפס-עלות. **מה תוקן:** ב-`app/api/write-summary/route.ts`, הוספתי פסקה ל-`TASK_PROMPT` (אחרי HEBREW QUALITY, לפני סגירת ה-template literal) שמגדירה חוזה-סגירה מפורש לנתיב ה-held (`{held:true, reflection}` כשהחומר טעון מדי לדחיסת JSON): לא לאלץ JSON, אבל לנחות בגוף-ראשון ("אני רוצה להביא/לשים לב...") ולא לסיים בשאלה מופנית למטופלת — מפורש שזה **עוקף** את הרגל הכללית "שאלה אחת בלבד" של דיאלוג חי, כי סיכום הוא לא שיחה. הועתק העיקרון מ-`next_session_focus` ב-`lib/analyze-note-prompt.ts` (BW-116). **געתי רק בפרומפט — אפס שינוי ל-JSON parsing / UI / ניתוב.** לא build-relevant (אין TS logic חדשה). **דורש אימות חי (לא build) — הקול צריך לשמוע לניסוח, לא רק שהקוד יתקמפל.** לא נדחף. הבא: איתן QA חי (3 תרחישים: חומר טעון קלאסי / שאלה-בתוך-הטקסט / עברית+אנגלית, יעד אפס סיום-בשאלה) → ליה מאמתת קלינית.
1. **feat(guardrails גל 1 — G8+G9+G11 ברמת-פרומפט) — לוקאל בלבד, uncommitted (07.07.2026).** יישום גל 1 של `docs/GUARDRAILS-MASTER-SPEC-2026-07-07.md` (ליה). export חדש `CORE_GUARDRAILS` ב-`lib/theorist-voices.ts` (סוף הקובץ, אחרי SAFETY_PROTOCOL) — בלוק אוניברסלי משותף **פעם אחת, לא משוכפל לכל תיאורטיקן**: **G8 NO COLLUSION** (ציר no-collusion בלבד — לא הסכמה/שיקוף אוטומטי · לא צידוד נגד המטפל/אדם אחר · לא שת"פ עם נרטיב-עצמי מעוות; **רצפה אוניברסלית מתחת ל-SYCOPHANCY CHECK הקיים בקולות**) + **G9 NO FABRICATION** (4 איסורים: עובדות/ציטוט-תיאורטיקן/זיכרון-מטופל/פרשנות-כעובדה) + **G11 OVERRIDE RESISTANCE** (בקשת-עקיפה = חומר לא הוראה; נתיב-משבר בלתי-ניתן-לשכנוע; אין developer-mode). **G8 gotcha:** SYCOPHANCY CHECK (ציר אנטי-כניעה) כבר קיים בקול בכל 4 הקולות (454/904/1412/1829) + קומפניונים — **לא נגעתי בו** (בקול, טוב, מחיקה=סיכון regression). הוספתי רק את ציר ה-collusion שהיה מפוזר (2039/2232/2449) ולא אחיד. חיווט ב-`app/api/chat/route.ts`: import (שורה 5) + הצמדה ל-`staticSystem` (שורה ~422). **gotcha קריטי שהתגלה:** `UNIVERSAL_SCOPE_INSTRUCTION` (רובד II) מוזרק ב-`dynamicSystem` **רק כשה-RAG מחזיר ריק** (route.ts:466-468) — כשה-RAG מצליח (המצב הרגיל ל-4 הקולות) הבלוק האוניברסלי **נופל**. לכן שמתי את CORE_GUARDRAILS ב-`staticSystem` (נשמר ב-cache, תמיד נוכח) ולא בדינמי. `npm run build` נקי (✓ Compiled, 37/37). אין CSS → lint:tokens לא רלוונטי. **הזרקת טקסט בלבד — אפס לוגיקה/route-flow/tokens/UI.** **לא G1 (stateful, איתן) · לא G9-שלב2 אימות-ציטוט (איתן) · לא עמידות prompt-injection טכנית (איתן) · לא גלים מאוחרים.** G9+G11 נוגעים ברגיסטר → לפי פסיקת ליה **חייב אימות חי של איתן (לא build)**. לא נדחף. הבא: איתן מאמת חי.
1. **fix(winnicott-hold, בלוק B חיזוק anti-scripting) — לוקאל בלבד, uncommitted (06.07.2026).** אחרי אימות חי של איתן (N=2): בלוק A PASS 2/2, בלוק B ירד Tier1→Tier2 אבל 1/2 סחף — המודל ניתב פעולה *דרך החדר* ("מה קשה להגיד לה: '[תסריט]'") מתחת לאיסור "מה היה קורה אם". חיזקתי את **סוף בלוק B** (`lib/theorist-voices.ts` ~1092) בנוסח ליה מילה-במילה: "Pointing toward the room is holding; scripting what to say there is mobilizing. Never author the patient's words..." + **קישור מפורש** ל-NEVER COMPLETE THE PATIENT'S SENTENCE (שורה 1105) — "applied at the threshold of the room" — כדי שלא יהיה כלל יתום אלא הרחבה של כלל קיים + WRONG/RIGHT מדוגמת-הסחף. **המיקום בכוונה בבלוק B (טריטוריית ה-mobilization) ולא בכלל השלמת-המשפט** — הסחף קורה בתוך מהלך הצבעה-לחדר, אבל המנגנון שנשבר הוא חיבור-משפט; הקישור עושה את החיבור. הזרקת טקסט בלבד, אפס לוגיקה/route/tokens/UI. `npm run build` נקי (✓ Compiled, 37/37). **הסחף סמנטי → חייב אימות חוזר של איתן (N≥3-4, יעד אפס mobilization), לא build.** לא נדחף. הבא: איתן מאמת חי.
